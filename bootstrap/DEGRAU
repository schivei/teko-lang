c:      bootstrap/teko.c
why:    the published release does not parse the 0.3.1 surface — `var` (no `let`/`mut`) and colon-return (`fn f(): T`, no `->`) — NOR the §14 `@Type()` macro pre-pass the §9.D sweep now uses in `src/`; it rejects the swept source, so bootstrap/teko.c is the bridge that builds it
since:  0.3.1.0
