c:      bootstrap/teko.c
why:    the published release does not parse the 0.3.1 surface — `var` (no `let`/`mut`) and colon-return (`fn f(): T`, no `->`) — NOR the §14 `@Type()` macro pre-pass the completed §9.D sweep uses pervasively in `src/`, NOR the §9.2b constraint grammar; and as of item-14 (§13) it no longer even shares the value-struct REPRESENTATION: every struct now carries the fat `tk_struct_hdr` self-pointer word and a struct method takes its receiver as that pointer (`self` is a ref), so a seed emitting thin structs cannot compile this tree into a working compiler. bootstrap/teko.c is the bridge that builds it
since:  0.3.1.0
