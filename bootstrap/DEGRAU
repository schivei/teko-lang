c:      bootstrap/teko.c
why:    the published release does not parse the 0.3.1 surface — `var` (no `let`/`mut`) and colon-return (`fn f(): T`, no `->`) — NOR the §14 `@Type()` macro pre-pass the §9.D sweep now uses in `src/`, including (§9.D CR3) an `@Type()` standing in a group-bind pattern position (`(@ns::Union()) as v`), its union-of-unions follow-on (a group-bind binding a NESTED union member), AND the §9.D `Type` retirement's marker dedup (`type_eq` equates the checker's own `Error`/`Null` type with the backend `Error{}`/`Null{}`, and `union_dedup` collapses the surface `| error`/`| null` a `@Type()` already carries); it rejects the swept source, so bootstrap/teko.c is the bridge that builds it
since:  0.3.1.0
