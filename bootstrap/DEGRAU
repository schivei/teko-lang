c:      bootstrap/teko.c
why:    the published release does not parse the 0.3.1 surface — `var` (no `let`/`mut`) and colon-return (`fn f(): T`, no `->`) — NOR the §14 `@Type()` macro pre-pass the completed §9.D sweep uses pervasively in `src/`: every retired `type X = variant` is now the inline union `@X()`, including a group-bind over a NESTED union member (`@TStatement()` inside `@TItem()`), and the checker's own central `type Type = variant …` is `@Type()` with the surface `| error` / `| null` deduplicated against the case the union already carries; it rejects the swept source, so bootstrap/teko.c is the bridge that builds it
since:  0.3.1.0
