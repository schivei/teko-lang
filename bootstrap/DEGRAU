c:      bootstrap/teko.c
why:    the published release does not parse the 0.3.1 surface — `var` (no `let`/`mut`) and colon-return (`fn f(): T`, no `->`) — NOR the §14 `@Type()` macro pre-pass the completed §9.D sweep uses pervasively in `src/` (every retired `type X = variant` is now the inline union `@X()`), NOR the §9.2b constraint grammar (form words `class`/`struct`/`service <lifetime>`, the `&`-only `notnull` modifier, and concrete-type terms, combined as a disjunction of conjunctions), which the regression corpus already writes and which §7 DI and §10 concurrency will write in `src/`; it rejects the swept source, so bootstrap/teko.c is the bridge that builds it
since:  0.3.1.0
