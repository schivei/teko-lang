c:      bootstrap/teko.c
why:    the published release does not parse the 0.3.1 surface — `var` (no `let`/`mut`) and colon-return (`fn f(): T`, no `->`) — NOR the §14 `@Type()` macro pre-pass the §9.D sweep now uses in `src/`, including (§9.D CR3) an `@Type()` standing in a group-bind pattern position (`(@ns::Union()) as v`) AND its union-of-unions follow-on, where a group-bind whose macro leaves a NESTED union member (`@TStatement()` inside `@TItem()`) binds that member without flattening; it rejects the swept source, so bootstrap/teko.c is the bridge that builds it
since:  0.3.1.0
