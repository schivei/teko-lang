c:      bootstrap/teko.c
why:    the published release does not parse the 0.3.1 surface — `var` (no `let`/`mut`) and colon-return (`fn f(): T`, no `->`); it rejects the swept source, so bootstrap/teko.c is the bridge that builds it
since:  0.3.1.0
