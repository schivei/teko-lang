# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): the §10 `spawn` batch — C0a maintained-C
# thread primitive + S1/S2/S3 compiler surface (parser/checker/codegen). What this harvest carries:
#   - **C0a (maintained-C)**: `src/runtime/teko_rt.{c,h}` — `tk_thread_spawn` (detached, fire-and-forget;
#     the arena bracket `tk_task_begin`/`tk_task_end` runs INSIDE a static pthread start-routine, the
#     `tk_test_run` setjmp-ownership precedent) + the await-batch twins `tk_thread_join_spawn`/
#     `tk_thread_join` (compiled, unused) + `tk_thread_spawn_selftest`. `#include <pthread.h>` on the
#     non-Windows branch (§16-TRANSITIONAL — thread creation goes to OS-FFI when teko_rt.c is retired).
#   - **S1 (compiler — parser)**: `Spawn`/`TSpawn` nodes into the `@Statement()`/`@TStatement()` unions;
#     contextual `spawn`+Ident recognition (`is_spawn_head`, modeled on `is_adopt_head`).
#   - **S2 (compiler — checker)**: `type_spawn` + the spawn-boundary ref-guard (reject a `ref` param
#     crossing the boundary — arena UAF) + copy-guard (reject uncopyable args).
#   - **S3 (compiler — codegen)**: per-site ctx-blob + `cabi` trampoline + arg DEEP-COPY (scalars inline,
#     `str`/slice bytes tail-packed with `ptr` re-pointed INSIDE the malloc'd block, so no spawning-arena
#     pointer survives); the trampoline frees the blob after the target returns. Native backend
#     honest-stops `TSpawn` (C backend is the target).
#
# NOT INERT — BY DESIGN, BUT NO BEHAVIORAL CORPUS DIVERGENCE. S1-S3 no-op the compiler's OWN corpus (no
# `.tks` in `src/` uses `spawn` — every `tk_spawn_*`/`tk_thread_spawn` token in the self-image is a
# codegen STRING TEMPLATE, never an actual call). The emitted C differs from the previous seed by the new
# parser/checker/codegen/runtime functions + mechanical temp-counter / source-loc shifts.
#
# GATE: SELF-REPRODUCE (a ONE-STEP LADDER, then a fixpoint).
#   - C-route chain: gen0 (cc, WITH `-pthread`, from the PREVIOUS bootstrap `3d98db74` — the F3 compiler,
#     which has NO spawn codegen) built the tree → emitted a TRANSITIONAL teko.c `639ab3b2` (tc1); tc1 ->
#     gen1 (the spawn-capable compiler); gen1 built the tree → tc2 `6cbfb13449415477a6a11f1447b867f2f8b
#     99984795e6fd99aac8ab6a0740aed`; tc2 -> gen2; gen2 built the tree → tc3 `6cbfb134…`. **tc1 != tc2**
#     (the one-generation ladder: the old F3 codegen numbers temps differently than the spawn compiler —
#     a temp-name cascade), but **tc2 == tc3, byte-identical** — the STABLE spawn generation reproduces
#     itself exactly. The seed is the STABLE generation (tc2 == tc3), NOT the transitional tc1.
#   - cc-route: cc -std=c2x -w -g -pthread -I src/runtime -I src/assert bootstrap/teko.c
#     src/runtime/teko_rt.c src/assert/assert.c -lm -> gen; TEKO_BACKEND=c gen . -o out --no-verify
#     --release -> out/teko.c; sha256(out/teko.c) == sha256(bootstrap/teko.c) == below.
#   - Regression: `examples/regressions/spawn_basic/` — four workers `spawn`ed, each writes a file named
#     by its copied `i64` tag with its deep-copied `str` content; main bounded-polls for exact content.
#     Runs exit 0 (`0.txt`..`3.txt` = `worker-0-hello`..`worker-3-hello`), threads ran and both scalar and
#     `str` args survived the boundary; clean under `TEKO_MEM_PARANOID=1`. C0a selftest (`examples/probes/
#     thread_spawn`) exits 42, `tk_names_live_count` drift 0 across 100 arena-per-thread cycles.
# The C bootstrap ladder gained `-pthread` on the non-Windows link (build_gen1_from_c, build_with_seed_
# fallback, native_linux_asset glibc+musl, package_release) so the C leg links `tk_thread_spawn`.
# Every capped build (`ulimit -v 6291456`) peaked under 5.8 GB, within the 6 GB cap.
#
# `run: hand-harvest` — no CI run produced this file: reseed-bootstrap.yml is not registered on the
# default branch, so it cannot be workflow_dispatch'd via the API; the harvest is LOCAL on this lane,
# forward-only, and its correctness rests on the self-reproduction above. Mirrors the prior harvests.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (judges on a SWAP).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  6cbfb13449415477a6a11f1447b867f2f8b99984795e6fd99aac8ab6a0740aed
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (the §10 spawn batch: C0a tk_thread_spawn
         + S1-S3 parser/checker/codegen), byte-identical on rebuild across the stable C-route generations
         (tc2 == tc3, the spawn-stable generation reached from 3d98db74 via one transitional tc1) AND on
         the cc-route re-emission; no unrecovered legacy artifact underlies it.
