# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): the FINAL §9.D self-host — the seed is
# now the compiler built from a tree in which ALL 32 `type X = variant` declarations have been retired to
# the inline union `@X()`, including the checker's own central `type Type`. This locks the completed sweep
# into the seed so CI's C lane bootstraps from a Type-retired gen0. It sits on top of the marker-dedup
# reseed (which sits on the union-of-unions group-option, CR1+CR2+CR3, §14 Form 2 parametric type-macros,
# §14 macro/comptime, §9.D, and the sweep). Standing reseed lesson: land the seed's new capability BEFORE
# the corpus uses it — every capability this seed needs (the §14 macro pre-pass, the CR1-CR3 rungs, the
# nested group-option, and the Error/Null marker dedup) entered the seed in an EARLIER reseed, so this one
# adds no new capability: it only advances the seed's OWN source to the finished corpus.
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh, which only
# ever judges this record on a SWAP.
#
# FINAL §9.D SELF-HOST reseed
# ---------------------------------------------------------------------------------------------
# WHAT CHANGED since the last reseed: `src/` retired the final two unions — `type TStatement = variant`
# (the first union-of-unions, whose `@TItem()` member stays nested and binds through the group-option) and
# `type Type = variant` (the checker's 14-case type model). No `bootstrap/teko.c` CAPABILITY is new here;
# the marker-dedup and nested group-option that these retirements exercise were both landed inert in prior
# reseeds. This harvest simply re-emits the seed from the now-complete tree so the committed gen0 is itself
# a `@Type()`-based compiler.
#   Carried forward: §9.D CR1/CR2/CR3, the union-of-unions nested group-option, the Error/Null marker
#   `type_eq` dedup + `union_dedup`, §14 A0-A4 / B1-B5, §14 Form 2 parametric type-macros, §9.D
#   capabilities (a)/(b)/(c). The sweep is COMPLETE: 32 of 32 `type X = variant` retired to `@X()`.
#
# FIXPOINT. The COMMITTED artifact SELF-REPRODUCES via the C route (the reseed-bootstrap.yml criterion):
#   cc -std=c2x bootstrap/teko.c -> gen (scripts/build_gen1_from_c.sh);
#   TEKO_BACKEND=c gen build . -> bin;  sha256(bin/teko.c) == sha256(bootstrap/teko.c) == below.
# Verified this run: the emitted teko.c, cc-compiled and used to rebuild the tip, re-emits itself
# byte-identically; and the C-route chain gen1 == gen2 == gen3 is byte-identical (binary sha
# 306120da…) at the same emitted-C sha. Every capped build (`ulimit -v 6291456`) peaked ~3.91 GB
# (well under the 6 GB cap).
#
# `run: hand-harvest` — no CI run produced this file: the canonical reseed-bootstrap.yml flow seeds from
# the RELEASED binary, which predates the 0.3.1 surface AND §14, so the harvest is LOCAL on
# fix/retirement, forward-only, and its correctness rests on the C-route self-reproduction above rather
# than a CI run-id. `commit: pending drain` — the landing commit is written in this same change.
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  6dd23da9ea5b670a3f4aa00ffc1f4c288bda30d6fe509fcdf09aa1fec01db205
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (the completed §9.D sweep: all 32
         `type X = variant` retired to `@X()`, on top of the union-of-unions group-option + Error/Null
         marker dedup + CR1+CR2+CR3 + §14 Form 2 parametric type-macros + §14 macro/comptime + §9.D),
         byte-identical on rebuild across three C-route generations AND on the cc-route re-emission; no
         unrecovered legacy artifact underlies it.
