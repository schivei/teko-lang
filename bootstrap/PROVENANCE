# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, by-hand per owner order): SELF-CONSTRUCTION R1 — the ADDITIVE `.{ … }`
# target-typed construction capability (§4.1). The owner did NOT edit bootstrap/teko.c by hand; the
# file is the harvested self-host output, proven a fixpoint. Format and parser: scripts/degrau.sh's
# kv_field; enforced by scripts/provenance_gate.sh, which only ever judges this record on a SWAP.
#
# self-construction R1 — target-typed `.{ … }` (§4.1), additive
# ---------------------------------------------------------------------------------------------
# WHAT ENTERS (docs/design/plano-self-construction.md, docs/design/mudancas-superficie-0.3.1.md §4.1):
#   - PARSER, in `src/parser/parse_expr.tks`: a leading `.` followed by `{` in `parse_atom` parses a
#     `.{ … }` construction as a `StructLit` carrying the ZERO-segment `dot_sentinel_path()` — an
#     unambiguous marker (every nominal or `self` prefix has >= 1 segment). Parsed UNCONDITIONALLY
#     (the leading `.` self-delimits, no `allow_struct` gate); the `self { … }` construct STILL parses
#     (R1 is additive — the removal is R2). Zero new AST field (the `.tkb` layout is unchanged).
#   - CHECKER, in `src/checker/typer.tks`: `is_dot_sentinel` + `dot_construct_target` resolve a
#     `.{ … }`'s target from the context's `expected` type (a concrete struct/class, an already-stamped
#     generic instance, or a generic self-return phantom `Foo__g__T` remapped by the mono pass); no
#     target ⇒ an honest "needs a known target type" error (`.{ … }` never infers by back-flow). The
#     canonical name is taken from `expected`, so the emitted `TStructInit` widens into the return/slot
#     by exact `type_eq` — closing the self-return gap the retired `self { … }` could not. A generic
#     owner's `-> self` return is now phantom-stamped (`self_return_named`) so a `.{ … }` self-construct
#     widens into it. `construct_target` gains the `.{ … }` branch alongside `self`/explicit/bare.
#   - RETURN-TARGET THREADING, in `src/checker/scope.tks` + `typer.tks`: `Env` gains `ret_expected`
#     (the body's return type, `Void` for a value-less body), set by `type_method`/`type_function` and
#     read at the two tail slots a `.{ … }` can occupy — a `return .{…}` value and a function's trailing
#     `.{…}` expression. The read is dot-sentinel-gated, so it is INERT on all existing (dot-free)
#     source. Arguments route a `.{ … }` through the parameter type (`expr_is_dot_construction`).
#   - `src/` PRODUCT UNCHANGED: the compiler's own sources still use `self { … }`/`Name { … }`; nothing
#     in `src/` uses `.{ … }` yet (the sweep + `self { … }` removal is R2). The new codegen path does
#     not exist — `.{ … }` lowers to the SAME `TStructInit` the nominal/`self` forms produce, so
#     codegen is untouched and the new parser/checker paths are corpus-inert on this source.
#
# ACCEPT fixture (examples/regressions/dot_construction): the seven `.{ … }` slots — a `static fn … :
# self` trailing value and `return`, an annotated binding, a call argument, a nested field, a generic
# self-return (phantom per width), and the coexisting NOMINAL `Name { … }` form. REJECT fixture
# (examples/regressions/dot_construction_reject, EXPECT_COMPILE_FAIL): a `.{ … }` in an un-annotated
# binding and a `.{ … }` as an `if` scrutinee, each a "needs a known target type" diagnostic.
#
# FIXPOINT. The COMMITTED artifact self-reproduces: cc(bootstrap/teko.c) -> gen0; gen0 builds src ->
# gen1 (emits teko.c); gen1 -> gen2 -> gen3, and all three emitted teko.c are BYTE-IDENTICAL
# (sha256 below). The new parser/checker paths are inert on the dot-free compiler source, so even the
# predecessor seed emits the stable C. A TEKO_MEM_PARANOID=1 build of the same source exits 0
# (self-reported peak 3630.2 MB — no regression against the pre-R1 3639.1 MB baseline). Every capped
# build (`ulimit -v 6291456`) peaked ~4.15 GB (tree, incl. cc), well under the 6 GB cap.
#   cc(bootstrap/teko.c) -> gen;  TEKO_BACKEND=c gen build . -> bin;  sha256(bin/teko.c) == below.
#
# `run: hand-harvest` — no CI run produced this file; harvested by hand on the branch, in the owner's
# ordered sequence. `commit: pending drain` — the landing commit is written in this same change.
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  e335871c2dbae247d7bb68f8f19bcfb2e0b1d2b07147131968140e5e1a28f841
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (the additive `.{ … }` target-typed
         construction capability of §4.1; `self { … }` still accepted, `src/` product unchanged),
         byte-identical on rebuild across three generations; no unrecovered legacy artifact underlies it.
