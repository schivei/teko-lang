# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh, which
# judges this record only on a SWAP (add/replace/remove) of bootstrap/teko.c.
#
# THIS SWAP (2026-08-03) — RESEED TO THE CONVERGED, NATIVE-MODEL-AWARE C. The incumbent it replaces
# was the retroactive main-era seed (harvested for PR #95, commit 4e6c4e4, _tkrr=0) — cut BEFORE
# the move-on-return / memory-model-by-scope work, and ~1181 commits stale by this tip. It is
# replaced here by fix/union's OWN converged C emission: the byte-identical output of the C-route
# fixpoint (gen2 == gen3) over the tip's source, which carries the native memory model (_tkrr=360).
#
# WHY THE RESEED. Beyond retiring a 1181-commit-stale seed, it measurably advances the native
# self-host: with this seed the native-lowering crash moves from item 229 to item 5083/6640
# (staleness was a real contributor). A residual native bug at teko::lir::lower_region_new_call
# remains and is tracked separately — this record only concerns the seed swap.
#
# HARVEST. The two-leg Theory CI probe of the tip (branch theory/at-tip, run below) built gen1
# through the declared degrau, ran the C-route fixpoint to a HELD gen2 == gen3, and staged
# .fixpoint/gen3.c — the exact bytes recorded here. run:/commit: name that run and the commit it
# measured; sha256 is read directly off the file this record describes.
c:       bootstrap/teko.c
run:     30855203762
commit:  7a1a49fbb40bd9676ede014ad21b1385c82a5a93
sha256:  84519be09ae9ae83b1ce649318b6d7a54be1dba2f4463e74f0c9e5518b34465f
since:   0.3.1.0
