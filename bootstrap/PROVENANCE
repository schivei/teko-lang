# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): the §16 arena crumb E, STEP L0 — the
# `cg_arena_sym` codegen indirection, a provable NO-OP refactor that de-risks the load-bearing L2 flip (the
# arena provider swap). What this harvest carries:
#   - **§16 crumb E / L0 (compiler — codegen refactor, no behavior)**: a new `CgArenaSym` enum (14 variants:
#     `RegionAlloc`/`RegionNew`/`RegionDrop`/`RegionDropSubtree`/`RegionRoot`/`RegionCurrent`/`RegionEnter`/
#     `RegionLeave`/`ArenaPush`/`ArenaPop`/`ArenaCommit`/`RegionRegister`/`RegionLookup`/`Alloc`) + `fn
#     cg_arena_sym(kind): str` in `src/codegen/codegen.tks`. Every one of the ~40 emit sites of an arena C-symbol
#     literal (call-site literals, interpolated fragments, bare "()" helpers, the `arena_push`/…-builtin name
#     table) now routes through `cg_arena_sym`, returning the SAME `tk_*` strings as before. One localized change;
#     the later L2 flip is a one-line change to this table's return values. Nothing else touched (no `arena.tks`,
#     no builtin retirement).
#
# NO BEHAVIOR CHANGE — PROVEN NO-OP. For EVERY program the compiler compiles, the OLD (pre-L0) and NEW (post-L0)
# compilers emit BYTE-IDENTICAL C (verified by the implementer on a standalone non-self-referential probe: `diff`
# empty, both run exit 0). The self-image's own `tk_region_alloc` source-literal count drops 18→~1 (now a single
# `cg_arena_sym` match arm) — a self-hosting META-EFFECT (Teko source string literals compile straight into the
# self-compiled C), NOT a behavior change; the arena is STILL the C `tk_region_alloc` (unwired — L2 flips it).
#
# GATE: SELF-REPRODUCE.
#   - C-route chain: gen0 (cc, WITH `-pthread`, from the PREVIOUS bootstrap `649eb6ed`) built the tree →
#     emitted teko.c `26b6ca48` (tc1); tc1 -> gen1; gen1 built the tree → tc2 `26b6ca48a39ad427d8c0d47278589207
#     73b79d83f242c70ccbc8e0c546e47ffc`; both byte-identical (coordinator-confirmed tc1 == tc2; implementer
#     additionally confirmed tc3 == tc2). Clean 3-gen fixpoint, no ladder (a pure indirection over the same
#     strings — behavior-inert; the fn/enum add shifts temp-var IDs, hence distinct from the previous seed).
#   - cc-route: cc -std=c2x -w -g -pthread -I src/runtime -I src/assert bootstrap/teko.c
#     src/runtime/teko_rt.c src/assert/assert.c -lm -> gen; TEKO_BACKEND=c gen . -o out --no-verify
#     --release -> out/teko.c; sha256(out/teko.c) == sha256(bootstrap/teko.c) == below.
#   - MEM_PARANOID: `TEKO_BACKEND=c TEKO_MEM_PARANOID=1 gen . -o out --no-verify --release` exits 0 (whole-tree,
#     implementer-confirmed). NOTE: the emit str-concat cap-boundary OOM (~5.6 GB / 6 GB cap) flaked one ladder
#     generation and passed on retry — a cap-boundary memory-pressure artifact (Doc-1 territory: the arrays-fixos
#     wave + AST pre-sizing cure it), NOT a memory-correctness violation.
# The self-reported peaks were ~5.67-5.83 GB (within the 6 GB cap; local ladder cc dropped `-g` purely to fit the
# coordinator container's RAM — the emitted seed bytes are cc-flag-independent).
#
# `run: hand-harvest` — no CI run produced this file: reseed-bootstrap.yml is not registered on the
# default branch, so it cannot be workflow_dispatch'd via the API; the harvest is LOCAL on this lane,
# forward-only, and its correctness rests on the self-reproduction above. Mirrors the prior harvests.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (judges on a SWAP).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  26b6ca48a39ad427d8c0d4727858920773b79d83f242c70ccbc8e0c546e47ffc
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (the §16 crumb E/L0 cg_arena_sym codegen
         indirection, a proven no-op refactor that routes the ~40 arena-symbol emit sites through one table),
         byte-identical on rebuild across the stable C-route generations (tc1 == tc2 == tc3, no ladder) AND on
         the cc-route re-emission; no unrecovered legacy artifact underlies it.
