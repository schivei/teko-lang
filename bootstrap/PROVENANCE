# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh, which
# only ever judges this record on a SWAP (an add, replace, or remove-then-reintroduce of
# bootstrap/teko.c) — never for the unchanged incumbent this record describes.
#
# §4 R1 — teach self/base/static, colheita à mão, ordem do dono (2026-08-11).
# ---------------------------------------------------------------------------------------------
# Hand-harvested for the FIRST sub-cycle of the §4 self/base/static work
# (docs/design/plano-secao4-self-base-static.md, Crumb 0 + Crumb 1). This bootstrap carries:
#   - Crumb 0: every plain-identifier use of `base` across the corpus renamed to a meaningful
#     per-site name (so `base` can be reserved without breaking the tree).
#   - Crumb 1: `self`/`base`/`static` reserved as keywords; `self`/`base` as primary expressions;
#     the `static` method modifier + `Function.is_static`; `self` as a type atom resolving to the
#     enclosing type (incl. a `self` return); the explicit-`self` receiver kept for back-compat;
#     the `base` super-receiver taught additively via the existing named-binding reinterpret.
# The §4 teach is a strict NO-OP over the corpus (the source does not yet USE the new keywords),
# verified by a green C-backend dry build under the previous bootstrap.
#
# FIXPOINT. The reseed converged in one step on this base: gen0 (cc of the previous bootstrap)
# C-built the current source into bin-a; compiling bin-a and rebuilding the source produced a
# byte-identical bin-b (sha256 below). The committed file is that fixpoint — it self-reproduces:
#   cc(bootstrap/teko.c) -> gen0';  TEKO_BACKEND=c gen0' build . -> OUT;  OUT/teko.c == bootstrap/teko.c.
#
# `run: hand-harvest` — no CI run produced this file; it was harvested by hand on the branch, in the
# owner's ordered §4 sequence. `commit: pending drain` — the landing commit is not yet known at
# harvest time (this record is written in the same change that commits the file).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  744bf541602bcef802a340d892718da33a92915f554d51e84b18a7e00b2082a0
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (§4 R1); sha256 is read directly off
         the committed file and was proven a fixpoint (bin-a == bin-b, byte-identical).
