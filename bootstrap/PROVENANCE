# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): §10 M2 CANONICALIZATION — the raw
# futex sync substrate and the thread-stack allocator move from their proving fixtures into their
# canonical `teko::runtime` surface home, now that the M2 atomic-RMW intrinsics are seeded
# (`docs/design/plano-s10-multithread-nativo.md` §6.2 / §4.1). What this harvest carries — all LEAF
# `.tks` plus ONE front-end wiring change, with ZERO `teko_rt.c`/`.h` edit (freeze law honored):
#   - **`src/runtime/sync.tks`** (namespace `teko::runtime`): the one-word 3-state futex mutex
#     (`mtx_lock`/`mtx_unlock`), the sequence condvar (`cv_wait`/`cv_signal`/`cv_broadcast`) and the
#     private `futex_wait`/`futex_wake` leaves, built purely in Teko over the seeded
#     `teko::sys::atomic_cas_u32`/`atomic_xchg_u32`/`atomic_add_u32`/`atomic_load_u32` intrinsics
#     (§10 R5) and the `SYS_FUTEX` wait/wake syscall.
#   - **`src/runtime/thread.tks`** (namespace `teko::runtime`): `thread_stack_new`/`thread_stack_free`
#     — a spawned thread's 2 MiB stack mapped over the raw `mmap` syscall with a `PROT_NONE` guard
#     page laid at the low end via `mprotect`, `#if(os == "linux")`-guarded with a non-Linux
#     type-check stub, mirroring `arena.tks`'s own `#os`-split discipline.
#   - **`src/sys/sys.tks`**: the `SYS_MPROTECT` number (x86_64 10 / aarch64 226) the guard page needs.
#   - **`src/build/project.tks`** (the ONE front-end wiring change): the runtime-prelude injector now
#     ships MULTIPLE files per namespace — `rt_prelude_source_path` (one path) becomes
#     `rt_prelude_paths_for` (a path list), and `inject_runtime_prelude` loops each so `teko::runtime`
#     injects `arena.tks` + `sync.tks` + `thread.tks` into every ordinary program. The dedup guard is
#     unchanged (per-namespace), so the compiler self-image — which DISCOVERS these files under its
#     own `src/runtime/` — is still skipped for injection.
#   - Because the self-image now discovers `sync.tks`, its own emitted teko.c gains the `tk_atomic_*`
#     RMW helper block (use-gated on the atomic call sites `sync.tks` introduces) and the dormant
#     sync/thread functions — the reason this leaf-canonicalization requires a reseed at all. The
#     spawn substrate is UNTOUCHED (still pthread): M3/M4 do not land in this harvest.
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (gen2 == gen3).
#   - C-route chain: gen0 (cc of the prior crumb-E seed) emitted the modified tree's teko.c == gen1's
#     re-emission (byte-identical); gen1 -> gen2 (RUNNING ON THE TEKO ARENA); gen2 -> gen3. The
#     fixpoint gate returns PASSED: gen2 == gen3 byte for byte, and gen2.c == gen3.c (22719448 bytes).
#     This harvested teko.c IS that stable emission (== gen1.c == gen2.c == gen3.c).
#   - The three canonicalized fixtures (`futex_mutex_counter`, `futex_condvar_barrier`,
#     `thread_stack_new`) build + run to exit 0 under the reseeded compiler, consuming the injected
#     canonical modules; `chan_memchan_spawn` and a diverse regression sample stay green (broad
#     injection is behavior-transparent — the injected sync/thread functions allocate nothing).
#   - `TEKO_MEM_PARANOID=1` full compiler self-build on the Teko arena exits 0 — the poison path ran
#     through a whole self-build with zero leak/use-after-free/live-count drift. Self-reported peak
#     5848.1 MB under paranoid (5754.6 MB plain).
#
# `run: hand-harvest` — no CI run produced this file: the harvest is LOCAL on this lane, forward-only,
# its correctness resting on the self-reproduction above. Mirrors the prior harvests.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (judges on a SWAP).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  89342ff07a2db80b6b09a45c59352062367c582fb96e8d3d32369a138563851b
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (the §10 M2 canonicalization: the
         futex sync substrate + thread-stack allocator as canonical teko::runtime surface, injected
         into every program), byte-identical on rebuild across the Teko-arena C-route generations
         (clean fixpoint gen2 == gen3, gen2.c == gen3.c); no unrecovered legacy artifact.
