# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on this lane, forward-only): re-harvest after the §7 DI-completion
# crumbs (docs/design/di-completion-crumb-plan.md) — CR-D (`req.is_static != impl_m.is_static` in
# `method_sig_matches`, src/checker/collect.tks), CR-C (the `IService` constraint atom now a
# predicate via `type_satisfies_iservice` in `constraint_atom_satisfied`, src/checker/monomorph.tks),
# and CR-A (`service_conflict_diags` — same-type-same-key service-registration conflict detection,
# hooked into `validate_type_decls`, src/checker/collect.tks). These are COMPILER changes to the
# corpus the self-build exercises, so they alter the emitted teko.c and the seed is re-harvested per
# CLAUDE.md ("Reseed de bootstrap/teko.c quando mudança de compilador altera o C emitido: harvest
# local (fixpoint gen2==gen3), sem gate de provenance"). No AST/wire field was added — CR-B (runtime
# fixtures) was dropped by owner ruling, so no reachable-syntax jump forced this; the previously
# committed bootstrap/teko.c already compiled this tree directly (no DEGRAU capability gap crossed by
# this reseed) and this harvest is a straight recompile-of-self after the three code edits above.
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (gen2 == gen3), backend C.
#   - gen0 linked straight from the PREVIOUS bootstrap/teko.c builds gen1 (this file); gen1 builds
#     gen2, gen2 builds gen3; gen1.c == gen2.c == gen3.c byte-identical (23 572 810 bytes) — the
#     compiler built from this seed rebuilds the tip and re-emits this seed byte for byte. Cross-
#     checked independently: the previously-fetched release-matching binary (0.3.0.31-beta) building
#     the same edited tip from scratch reproduces the identical sha256.
#   - MEMORY: gen1/gen2/gen3 C-route build peak ~3.28-3.30 GiB (TEKO_BACKEND=c).
#
# `run: hand-harvest` — LOCAL on this lane, forward-only, resting on the clean fixpoint above.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (DISABLED
# per CLAUDE.md — the seed is accepted by force; these comments do not gate).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  694a1b2d7cf8c40df1e3ae959f2cfbeffabe350b698a53666d7d5bcd88795aaf
since:   0.3.1.0
legacy:  b3a45b4478dea0ef9e9f4a7f2bbfd0ae53fec4b53da8343b5ff75de6b38d27f2 — self-reproducible harvest
         of the §7 DI-completion crumbs (CR-D/CR-C/CR-A); byte-identical on rebuild (clean fixpoint
         gen2 == gen3, gen2.c == gen3.c, gen1.c == gen2.c, 23 572 810 bytes).
