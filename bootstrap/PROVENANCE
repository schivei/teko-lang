# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): the §16 arena sub-crumb C — the `word_ptr`
# intrinsic (i64 -> ptr), the INVERSE of `ptr_word`. It completes the three raw primitives the allocation-free
# arena core needs: the arena bumps a cursor in integer space and must hand each allocation back AS a `ptr` (so
# codegen's existing `(uint8_t *)tk_region_alloc(…)` cast + region-handle ABI keep compiling). What this harvest
# carries:
#   - **§16 arena crumb C (compiler — checker + codegen)**: `teko::sys::word_ptr(w: i64): ptr`:
#     - Checker (`scope.tks`, next to `ptr_word`): `Func { params = <[i64]>; ret = Ptr { inner = null } }`; the
#       opaque `ptr` widens at the use-site via the existing `ptr_widens_to_opaque` path (like `buf_ptr`).
#     - Codegen (`emit_word_ptr`, beside `emit_ptr_word`): `((void *)(uintptr_t)(<w>))` — a SPECIAL emitter (it
#       needs the cast), dispatched by bare last segment `word_ptr`. Like `ptr_word`/`ref_word`/`f64_bits`, it is
#       a compiler INTRINSIC (no surface `to`), the sanctioned reinterpret carve-out; `cast_check`/the opaque-ptr
#       law stay intact (no user-facing `u64 -> ptr` cast is introduced).
#     - Native leg: HONEST-STOP (falls into `lower_call`'s terminal `_ =>`, as `ptr_word`/`ref_word` do).
#
# NOT INERT IN SOURCE, BUT NO BEHAVIORAL CORPUS DIVERGENCE. The compiler's own corpus (`src/`) has ZERO
# `word_ptr` call sites, so the new emitter never fires on the self-image; the emitted C differs from the
# previous seed only by the new checker/codegen SOURCE plus the mechanical line/offset shifts it induces (a
# function-add reseed). The `arena_wordptr` fixture is the only invoker.
#
# NATIVE LEG — honest-stop; this crumb targets the C leg. Native lower is Doc-2 terminal.
#
# GATE: SELF-REPRODUCE.
#   - C-route chain: gen0 (cc, WITH `-pthread`, from the PREVIOUS bootstrap `7bc8eac4`) built the tree →
#     emitted teko.c `2fcb31b0` (tc1); tc1 -> gen1; gen1 built the tree → tc2 `2fcb31b0b9b666e4c272db468b34097f
#     cb4c38668604d4582cc37d68ebfab108`; tc2 -> gen2; gen2 built the tree → tc3 `2fcb31b0…`. tc1 == tc2 == tc3,
#     byte-identical (sha256 below). No ladder: the corpus invokes no `word_ptr`, so gen0 and gen1 emit the
#     self-image identically at tc1.
#   - cc-route: cc -std=c2x -w -g -pthread -I src/runtime -I src/assert bootstrap/teko.c
#     src/runtime/teko_rt.c src/assert/assert.c -lm -> gen; TEKO_BACKEND=c gen . -o out --no-verify
#     --release -> out/teko.c; sha256(out/teko.c) == sha256(bootstrap/teko.c) == below.
#   - MEM_PARANOID: `TEKO_BACKEND=c TEKO_MEM_PARANOID=1 gen . -o out --no-verify --release` exits 0 (whole-tree,
#     clean through emit+cc). As with the prior crumbs, a first capped run tripped the arena's own "out of memory
#     (str concat)" guard at the emit at the ~5.6 GB / 6 GB-cap boundary and a re-run completed exit 0 — a cap-
#     boundary memory-pressure artifact (Doc-1 territory), NOT a memory-correctness violation.
#   - Regression: `examples/regressions/arena_wordptr/` — round-trips `word_ptr(ptr_word(as_cstr("x")))` and
#     writes 1 byte through the RECOVERED pointer via `syscall3(SYS_WRITE, 1, ptr_word(back), 1)`; the built
#     binary prints `x` and exits 0. Proves `word_ptr` yields a working pointer (crumb C = the arena's outgoing
#     address bridge).
# The self-reported peak of the whole-tree build was ~5.6-5.8 GB (within the 6 GB cap; local ladder cc dropped
# `-g` purely to fit the coordinator container's RAM — the emitted seed bytes are cc-flag-independent).
#
# `run: hand-harvest` — no CI run produced this file: reseed-bootstrap.yml is not registered on the
# default branch, so it cannot be workflow_dispatch'd via the API; the harvest is LOCAL on this lane,
# forward-only, and its correctness rests on the self-reproduction above. Mirrors the prior harvests.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (judges on a SWAP).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  2fcb31b0b9b666e4c272db468b34097fcb4c38668604d4582cc37d68ebfab108
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (the §16 arena crumb C: the word_ptr intrinsic,
         i64 -> ptr, the inverse of ptr_word that lets the arena core hand back allocated pointers; inert on the
         corpus that invokes no word_ptr), byte-identical on rebuild across the stable C-route generations (tc1
         == tc2 == tc3, no ladder) AND on the cc-route re-emission; no unrecovered legacy artifact underlies it.
