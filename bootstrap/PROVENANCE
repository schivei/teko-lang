# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, by-hand per owner order): re-harvested after landing §5 Part A of the 0.3.1
# surface migration — opaque atomic `ptr`/`uptr` + `__wrap`/`__unwrap`. This is a checker/codegen
# change, so the compiler shifts and the previous bootstrap no longer reproduces it; the new harvest
# e936eba0 self-reproduces byte-identical (gen1==gen2 fixpoint). The owner did NOT edit
# bootstrap/teko.c itself; this reseed only realigns the seed with the current source.
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh, which
# only ever judges this record on a SWAP (an add, replace, or remove-then-reintroduce of
# bootstrap/teko.c) — never for the unchanged incumbent this record describes.
#
# §5 part A: opaque ptr/uptr + __wrap/__unwrap surface (seams open for doc1)
# ---------------------------------------------------------------------------------------------
# `ptr`/`uptr` become OPAQUE ATOMIC words (a single machine word carrying only an address; C-repr
# stays pointer-width `void*`/`uintptr_t` today, alignable to the future `size`/`usize`). `ptr<T>` is
# retired: `Ptr.inner` is now always `null`, and the generic annotation `ptr<T>`/`uptr<T>` is rejected
# in `resolve_generic_inst`. Four intrinsics are recognized in the TYPER as meta-type operations (like
# `type_region_alloc`), with NO new grammar:
#   - `ptr::__unwrap<T>(x)` / `uptr::__unwrap<T>(x)` — STATIC, infallible: takes the address of an
#     addressable `mut` variable (R7 ordinary auto-ref) and yields the opaque word.
#   - `p.__wrap<T>()` — INSTANCE, fallible: returns the FINAL union `T | error | null`. The `null` arm
#     (address 0) is LIVE; the `error` arm is emitted+compiled but DEAD in Part A (the codegen guard is
#     the literal `1`).
# Codegen adds `emit_ptr_unwrap`/`emit_ptr_wrap` plus the two BACKEND SEAMS `emit_ptr_unwrap_seam`
# (Part A: empty) and `emit_ptr_wrap_guard` (Part A: emits `1`) in src/codegen/codegen.tks — the ONLY
# two function bodies Doc 1 (Part B) swaps for `tk_ptr_track`/`tk_ptr_check` + the runtime; no
# signature, typer, type, or fixture changes between A and B. Arithmetic/deref/borrow/subscript on
# `ptr`/`uptr` are rejected (`type_binary`/`type_unary`/`type_index`/`type_borrow_expr`). The seven
# internal `ptr<T>` sites were swept to the opaque `ptr` (region_alloc return; RawBuf.ptr; the FFI
# prims buf_ptr/bytes_from_ptr/as_ptr/as_cstr/str_from_c/region_buf; the `ptr <-> u64` address-carrier
# cast; the extern absence union; the export honest-stop text), and tkb type-tag 12 (`ptr<T>`) was
# removed (10 = opaque `ptr`, 11 = `uptr` remain). ZERO runtime C touched — no new `tk_*` symbol.
#
# FIXPOINT. The reseed converged in one step: gen0 (cc of the previous bootstrap) C-built the reworked
# source into bin-a; compiling bin-a into the new compiler and rebuilding produced a byte-identical
# bin-b (sha256 below). The committed file is that fixpoint — it self-reproduces:
#   cc(bootstrap/teko.c) -> gen0';  TEKO_BACKEND=c gen0' build . -> OUT;  OUT/teko.c == bootstrap/teko.c.
#
# `run: hand-harvest` — no CI run produced this file; it was harvested by hand on the branch, in the
# owner's ordered sequence. `commit: pending drain` — the landing commit is not yet known at harvest
# time (this record is written in the same change that commits the file).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  2a70007f39c06d245d4788527e093ae5fe8adbaf452616a186bf9e5597e58897
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (§5 part A: opaque atomic ptr/uptr +
         __wrap/__unwrap surface, seams open for doc1); sha256 is read directly off the committed file
         and was proven a fixpoint (bin-a == bin-b, byte-identical).
