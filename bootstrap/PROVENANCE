# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): re-harvest after the §9.D
# retirement-lane fix that completes serialize_const's dispatch over inlined `@Type()` unions
# (src/lir/lower_const.tks). The fix adds a `Variant` arm (and factors const_variant_from_members
# out of const_variant_bytes) so a const of a §9.D macro-union type — RET_INST: @MInst() = MRet { },
# whose resolved cd.ty is a structural checker::Variant rather than the former Named "MInst" —
# serializes its variant tag instead of falling through to the `_` honest-stop (the #594
# const-aggregate error that broke the native self-host and the fixpoint). This is a COMPILER change
# to the corpus the self-build exercises, so it alters the emitted teko.c and the seed is re-harvested
# per CLAUDE.md ("Reseed de bootstrap/teko.c quando mudança de compilador altera o C emitido: harvest
# local (fixpoint gen2==gen3), sem gate de provenance").
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (gen2 == gen3), backend C.
#   - gen1 builds gen2, gen2 builds gen3; gen2 == gen3 byte-identical (binary), gen2.c == gen3.c
#     byte-identical (23 566 754 bytes); gen1.c == gen2.c == gen3.c, and this file IS that stable
#     output — the compiler built from this seed rebuilds the tip and re-emits this seed byte for byte.
#   - MEMORY: gen2/gen3 C-route build peak ~3.31 GiB (TEKO_BACKEND=c).
#
# `run: hand-harvest` — LOCAL on this lane, forward-only, resting on the clean fixpoint above.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (DISABLED
# per CLAUDE.md — the seed is accepted by force; these comments do not gate).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  b3a45b4478dea0ef9e9f4a7f2bbfd0ae53fec4b53da8343b5ff75de6b38d27f2
since:   0.3.1.0
legacy:  none — self-reproducible harvest of the #594 serialize_const Variant-arm fix; byte-identical
         on rebuild (clean fixpoint gen2 == gen3, gen2.c == gen3.c, gen1.c == gen2.c, 23 566 754 bytes).
