# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, by-hand per owner order): re-harvested after the owner's `->`→`:` return
# refac (commit 408171af) shifted an emitted diagnostic string, so the committed source no longer
# compiled to the previous bootstrap (b02cf8e4). New harvest b84f482d self-reproduces byte-identical
# (gen1==gen2 fixpoint), verified independently from a clean archive. The owner did NOT edit
# bootstrap/teko.c itself; this reseed only realigns the seed with the current source.
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh, which
# only ever judges this record on a SWAP (an add, replace, or remove-then-reintroduce of
# bootstrap/teko.c) — never for the unchanged incumbent this record describes.
#
# §4 R3 — strip self explícito + des-ensino receptor solto/super-binding; grafia nova pura
# (2026-08-11). FINAL sub-cycle of §4: the compiler now rebuilds itself 100% in the new spelling.
# ---------------------------------------------------------------------------------------------
# Hand-harvested for the THIRD and LAST sub-cycle of the §4 self/base/static work
# (docs/design/plano-secao4-self-base-static.md, Crumb 6 + 6b + Crumb 7). This bootstrap carries,
# on top of R2 (statics marked + synthetic-`self` injection is the parser default):
#   - Crumb 6 (SWEEP-B): the explicit `self` receiver (param[0], untyped, named `self`) STRIPPED
#     from every instance-method DECLARATION in the corpus (54 sites: 43 in `.tks` — rawbuf, iter,
#     io/stream+file_stream+compress_stream, collections/map+list — and 11 in `.tkt` fixtures). Only
#     the receiver slot was removed; the `self` in method BODIES stays (it now refers to the
#     synthetic receiver the parser injects). String/comment occurrences were left untouched.
#   - Crumb 6b (G, super call-sites → `base`): ZERO sites in the corpus (no `class Base(name)`, no
#     `name.super()`); a no-op sweep confirmed by grep.
#   - Crumb 7 (DES-TEACH): the OLD spelling now ERRORS (§12.1). (a) `parse_params` lost
#     `allow_untyped_first` and the loose-receiver branch — an untyped first param, or a written
#     `self`, is a parse error; `parse_function`'s `allow_receiver` became `is_type_method` (its sole
#     remaining duty is injecting the synthetic receiver for a non-`static` member). (b) the NAMED
#     super-binding was retired: `parse_class_body` no longer parses `class Base(name)` (a `(` there
#     is now an error — super is reached via `base`), and the checker's
#     `has_base_binding`/`base_binding_name`/`inject_base_binding` branch is gone; `base` injection is
#     regated on the class HAVING a base (`base_name != ""`) and is the SOLE super path. The AST
#     fields `has_base_binding`/`base_binding_name` (ClassBody) are kept VESTIGIAL (always false/"")
#     so the `.tkb` serialization format is untouched; their physical removal is a W15 pass post-§4.
# R3 is a strict NO-OP over the corpus behavior: the source was already stripped of every explicit
# receiver (Crumb 6) and carries 0 base-bindings, so removing the acceptance of both breaks nothing.
# Verified by a green C-backend dry build under R2.
#
# FIXPOINT. The reseed converged in one step on this base: gen0 (cc of the R2 bootstrap) C-built the
# des-taught source into bin-a; compiling bin-a into the R3 compiler and rebuilding the source
# produced a byte-identical bin-b (sha256 below). The committed file is that fixpoint — it
# self-reproduces:
#   cc(bootstrap/teko.c) -> gen0';  TEKO_BACKEND=c gen0' build . -> OUT;  OUT/teko.c == bootstrap/teko.c.
# With R3, §4 is COMPLETE: the compiler rebuilds itself entirely in the new spelling — synthetic
# `self`, `static` members, `base` super, and `self` as a return type.
#
# `run: hand-harvest` — no CI run produced this file; it was harvested by hand on the branch, in the
# owner's ordered §4 sequence. `commit: pending drain` — the landing commit is not yet known at
# harvest time (this record is written in the same change that commits the file).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  b84f482dbe81877c07bb0f2f04dccb7d2ac956393797b2a76cbc8323cea6d626
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (§4 R3); sha256 is read directly off
         the committed file and was proven a fixpoint (bin-a == bin-b, byte-identical).
