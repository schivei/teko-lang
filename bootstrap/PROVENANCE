# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): the NATIVE-backend const-struct
# `__hdr` fix lands in the LIR const lowering (`src/lir/lower_const.tks`), so the compiler's own
# emitted teko.c gains the new guard. What this harvest carries:
#   - **native const-struct `__hdr` guard (compiler — LIR)**: item-14 (§13.2) prepends a synthetic
#     pointer-sized `__hdr` self-reference as the FIRST member of every value-struct layout, in both
#     backends. The C backend leans on C's zero-init of unspecified compound-literal fields; the
#     NATIVE const path iterates the FULL layout (including `__hdr`) and looked each field up in the
#     user's `TStructInit` via `struct_init_value`. The user never binds `__hdr`, so a const struct
#     raised `const struct: initializer binds no field \`__hdr\` (internal)` on the native leg.
#       * Fix (`const_struct_field_into`): when the field is `struct_hdr_field_name()` (`__hdr`),
#         skip the user-initializer lookup and write `ltype_size(LType::Ptr)` zero bytes at the
#         field's offset via a new `const_hdr_field_into` helper — mirroring the non-const path
#         (`alloca` zero-inits the struct, only user fields are written) and the C backend (the
#         unspecified header member zero-fills). No ABI change; `__hdr` stays zero on both engines.
#   - Touch points: `src/lir/lower_const.tks` (the `const_hdr_field_into` helper + the guard clause).
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (tc1 == tc2 == tc3).
#   - The committed seed already carries the new guard, so gen0 (cc of this seed) and gen1 emit the
#     same whole-tree bytes: the seed `441c6318` converges on its own image in ONE pass.
#   - C-route chain: gen0 (cc, `-pthread`, from bootstrap `441c6318`) built the tree → tc1
#     `441c6318…`; tc1 -> gen1; gen1 built the tree → tc2 `441c6318…`; tc2 -> gen2; gen2 built the
#     tree → tc3 `441c6318…`. tc1 == tc2 == tc3, byte-identical (sha256 below). Verified locally:
#     gen1's emitted C == gen2's emitted C == this seed, and gen1 binary == gen2 binary; the
#     fixpoint gate returns PASSED (gen2 == gen3 byte for byte, gen2.c == gen3.c).
#   - cc-route: cc -std=c2x -w -pthread -I src/runtime -I src/assert bootstrap/teko.c
#     src/runtime/teko_rt.c src/assert/assert.c -lm -> gen; TEKO_BACKEND=c gen . -o out --no-verify
#     --release -> out/teko.c; sha256(out/teko.c) == sha256(bootstrap/teko.c) == below.
#   - The bug is NATIVE-only and cannot be exercised on the Linux/C leg; the fix is correct by
#     construction (it mirrors the non-const and C paths), and CI's macOS native fixpoint is its
#     terminal proof. Self-reported peak ~5.7 GB (within the 6 GB cap).
#
# `run: hand-harvest` — no CI run produced this file: reseed-bootstrap.yml is not registered on the
# default branch, so it cannot be workflow_dispatch'd via the API; the harvest is LOCAL on this lane,
# forward-only, and its correctness rests on the self-reproduction above. Mirrors the prior harvests.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (judges on a SWAP).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  441c6318c4bbe39ba03c97e857e1908ac45cdc5324cb6a76d5d92613145ab34a
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (the native const-struct `__hdr`
         guard in the LIR const lowering — skip the synthetic header field in the user-initializer
         lookup and zero-fill it at its offset), byte-identical on rebuild across the stable C-route
         generations (clean fixpoint tc1 == tc2 == tc3) AND on the cc-route re-emission; no
         unrecovered legacy artifact.
