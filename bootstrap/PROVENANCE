# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on feat/arrays-reclaim-first, forward-only): marco 5 of the
# io-streaming / array-reclaim campaign — the second memory-reduction batch, driven by the arena
# lifetime map (TEKO_ARENA_OBS). Converts the next tier of push copy-grow leak sites from
# teko::list::push accumulation to preallocated [n]T index-assign (the MAP / merge natures):
#   - checker::resolve_type: UnionType members and FunctionType params.
#   - checker/nidx.tks: nidx_merge (merge, size = left+right), nidx_msort base case, nidx_build.
#   - checker::subst_type: Variant members and Func params.
#   - checker::type_param_table + concat_tps/concat_tcs + tt_last_names (tt_push loop replaced by a
#     single preallocated regs array of the known final size).
#   - codegen::cg_te_to_type_ns: UnionType members.
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (gen2 == gen3), backend C, CC=clang.
#   - gen2 builds gen3; gen2.c == gen3.c byte-identical; this file is that stable output.
#   - MEMORY: gen2 peak VmHWM 5.48 -> 4.47 GiB (VmRSS 5.22 -> 4.26 GiB); cumulative from the
#     campaign start 6.46 -> 4.47 GiB. Guard: builds ran under `ulimit -v 10485760` (10 GiB dev
#     allowance) with TEKO_BACKEND=c.
#
# `run: hand-harvest` — LOCAL on this lane, forward-only, resting on the clean fixpoint above.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (DISABLED
# per CLAUDE.md — the seed is accepted by force; these comments do not gate).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  4e024d11bb3282ed0672d36399addc7af51fd5148a61a1cc60afbc31c76c8971
since:   0.3.1.0
legacy:  none — self-reproducible harvest of marco 5 (push->preallocate batch in resolve_type/nidx/
         subst_type/type_param_table/cg_te_to_type_ns, gen2 peak 5.48 -> 4.47 GiB); byte-identical
         on rebuild (clean fixpoint gen2 == gen3, gen2.c == gen3.c, 23 249 632 bytes).
