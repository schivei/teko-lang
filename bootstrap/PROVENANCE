# HAND HARVEST, 2026-08-11 (explicit owner order — overrides scripts/provenance_gate.sh's CI-green
# rule for this one swap). This bootstrap/teko.c was harvested BY HAND for the 0.3.1 SURFACE reseed:
# §1 `let`/`mut` → `var` (every mutable local is `var`), and §2 `->` → `:` on function return AND on
# the function TYPE `(A,B)->R` → `(A,B):R`, swept across all .tks/.tkt, plus the un-teach in the
# lexer/parser (token.tks drops `Arrow`; the lexer no longer recognizes `->`; `let`/`mut` are now a
# parse-time retirement error). Because the published release can no longer parse this surface, the
# harvest could not run the normal CI-green ceremony; it was done by hand per the owner:
#   1. a both-forms BRIDGE seed (accepts old AND new spelling, incl. the missing teach of
#      parse_function_type) compiled the swept source -> a new-spelling harvest (reseed#2);
#   2. that harvest, compiled to a NEW-SPELLING-ONLY binary, rebuilt the source (reseed#3) BYTE-
#      IDENTICAL to reseed#2 — a proven fixpoint (gen2 == gen3), so the source carries no stray old
#      spelling and the seed is stable. `sha256:` below is reseed#2's teko.c, exact off disk.
#
# THIS IS A GENUINE HARVEST — NO HAND-EDITS. The STATIC-LADDER HAND-EDIT block below (2026-08-05,
# the three clang-on-Windows surgical edits) is now OBSOLETE: the compiler source carries the
# clang-Windows logic NATIVELY — src/build/project.tks has `const WINDOWS_CC_NAME = "clang"`,
# `cc_family_is_clang`, and `const WINDOWS_MSVC_TRIPLE = "x86_64-pc-windows-msvc"` — so nothing had
# to be patched into the emitted C by hand this time. `run:` is `hand-harvest` (no CI run measured
# it); `commit:` is `pending drain` until this reseed lands on fix/retirement (no sha invented).
#
# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh, which
# only ever judges this record on a SWAP (an add, replace, or remove-then-reintroduce of
# bootstrap/teko.c) — never for the unchanged incumbent this record describes.
#
# THIS RECORD IS RETROACTIVE, written 2026-07-29, and says so honestly rather than inventing a
# live harvest that never happened for this file. bootstrap/teko.c was committed by PR #95
# ("ci(.31): vagão 20 — lanes por artefato, lane de artefato como raiz, RUNG 0 e higiene do
# regressor"), landing on main as commit 4e6c4e4 — before scripts/provenance_gate.sh, and before
# the harvest-then-record ceremony this file's format exists to enforce, both existed.
#
# WHAT `run:` NAMES AND HOW IT WAS FOUND. `gh api repos/schivei/teko-lang/commits/.../check-runs`
# and a direct WebFetch of `api.github.com/repos/schivei/teko-lang/commits/.../check-runs` both
# refused this session with HTTP 403 ("GitHub access is not enabled for this session. An org
# admin must connect the Claude GitHub App for this organization.") — the Claude proxy
# restriction the owner anticipated, not a failure to look. The PUBLIC, unauthenticated checks
# page at https://github.com/schivei/teko-lang/pull/95/checks rendered without that restriction
# and named GitHub Actions run 30381332577 for the "Pull Request CI" workflow — the same
# workflow name scripts/originating_pr_run.sh already treats as this repository's consolidated
# `pull_request` gate — triggered by a `synchronize` event on PR #95, on branch
# `ci/0.3.1-lanes-e-seeds`, concluded SUCCESS. That is the run this record names.
#
# WHAT `commit:` NAMES AND WHY IT IS NOT THE EXACT SHA THAT RUN MEASURED. Under squash-merge
# (scripts/originating_pr_run.sh's own header explains the general case) the commit CI actually
# checked out is the PR's pre-squash HEAD, a sha distinct from the landing commit and not part
# of this repository's own history — GitHub's PR head ref for a merged, squashed PR is routinely
# deleted, and the truncated, JS-rendered commits list on the PR page did not yield it to a
# WebFetch without API access. `commit:` therefore names 4e6c4e4, the LANDING commit that
# actually carries this exact tree in THIS repository's own, permanently resolvable history —
# the closest verifiable identifier available, not the literal sha the run's checkout used.
#
# CONFIDENCE. `c:` and `sha256:` are read directly off the file this record describes — exact,
# not reconstructed. `run:` and `commit:` are a best-effort attribution built from public,
# unauthenticated GitHub pages after every authenticated route was refused; nothing here was
# guessed or invented to make this record LOOK complete — where a fact was not recoverable this
# record would name that plainly, the way `legacy:` does for the parts that stayed short of full
# certainty.
#
# STATIC-LADDER HAND-EDIT, 2026-08-05 (owner ruling: "faremos uma escada estática"). This file is
# no longer a verbatim harvest of run 30381332577: three surgical edits were applied by hand to
# make gen0 (the degrau compiler this file becomes) build gen1 with CLANG on Windows instead of
# MinGW gcc — MinGW was measured at ~55 min for one produce step (its -O2 optimizer is superlinear
# on this 10 MB single-TU C), its separate `cc1` backend breaks under any PATH wrapper
# ("cannot execute 'cc1': CreateProcess"), and its MSVC-family linker rejects `-lm` ("cannot open
# 'm.lib'"). The edits, all keyed off `tk_rt_os() == "windows"` (the idiom already used elsewhere
# in this file): (1) `default_cc_for_target` for X8664WINDOWS now returns "clang", not
# "x86_64-w64-mingw32-gcc"; (2) `resolve_cc`'s host fallback returns "clang" on Windows, "cc"
# elsewhere; (3) `build_cc_argv` skips `-lm` on Windows (mirroring the tip's `append_libm_flag`);
# (4) `build_cc_argv` passes `--target=x86_64-pc-windows-msvc` on Windows so clang targets MSVC
# explicitly rather than relying on the runner's default.
# clang needs no `-Wno-error` here — it treats the incompatible-pointer class as a warning, which
# the existing `-w` already silences. `run:`/`commit:` below still name the harvest this file
# DERIVES from; `sha256:` is the post-edit digest, exact off disk.
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  d1ab97b236be7f97e99af0bb47bc23a3bb25bdb57a5ee57067da0e8bfe80b636
since:   0.3.1.0
legacy:  retroactive reconstruction (2026-07-29) — see the comment above for what is exact and
         what is a best-effort attribution, and exactly which authenticated GitHub routes were
         refused (403) before falling back to the public checks page.
