# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): the §17 CONDITIONAL-COMPILATION machinery
# (parser + build-time prune). What this harvest carries:
#   - **§17 (compiler — parser + build prune)**: build-time conditional compilation.
#     - Crumb A: the predicate AST (`@PredKind()` = `PTrue|PEq|PFlag|PNot|PAnd|POr`, `Pred` wrapper) +
#       a pure boolean-over-constants evaluator (`src/build/prune.tks`: `CcEnv`, `eval_pred`) — short-
#       circuit, total, unknown-axis/absent-flag → false; strictly a PRUNE evaluator, not §14 comptime.
#     - Crumb B: `parser::Item` gained `guard: Pred` (prune-time only, erased before `.tkb`); `Module`
#       gained `decl_guards`; `prune_cc` (driven by `CcEnv{target_os, target_arch, build_flags}`)
#       replaces `prune_os` at the same `frontend_check` slot (before macros/deps/type-check). `#os`
#       keeps `Function.os_guard` for `.tkb`/merge byte-compat.
#     - Crumb C: `#arch("…")` attribute (canonical `"arm64"`; the sealed-spec `"arch64"` was a typo).
#     - Crumb D: `#os`/`#arch`/`#if` widened to ALL top-level items (`fn`, `type`, `flags`, `const`,
#       `extern`), not just functions.
#     - Crumb E: `#if/#elseif/#else/#endif` regions + composite predicate parser (`src/parser/parse_cc.tks`,
#       `||`/`&&`/`!`/grouping), first-true-wins as AND-of-NOTs, nested regions recurse.
#     - Crumb F: `build_flags(m)` honest-stops empty (no flag source invented) → `PFlag` soundly false.
#
# NOT INERT — BY DESIGN, BUT NO BEHAVIORAL CORPUS DIVERGENCE. §17 no-ops the compiler's OWN corpus: `src/`
# applies ZERO `#os`/`#arch`/`#if` guards, so every item's guard is `PTrue` and the prune is a no-op over
# the tree → emitted C byte-identical PER PLATFORM (same platform → same survivors → same emit). The
# emitted C differs from the previous seed only by the new parser/build source + mechanical shifts.
#
# GATE: SELF-REPRODUCE.
#   - C-route chain: gen0 (cc, WITH `-pthread`, from the PREVIOUS bootstrap `6cbfb134`) built the tree →
#     emitted teko.c `20d7cb9b` (tc1); tc1 -> gen1; gen1 built the tree → tc2 `20d7cb9b9cda38563d160e8da0
#     cc621b2ecced97431348b92c89fac266fe7539`; tc2 -> gen2; gen2 built the tree → tc3 `20d7cb9b…`. tc1 ==
#     tc2 == tc3, byte-identical (sha256 below). No ladder this harvest: the guard-free corpus makes the
#     prune a no-op, so the OLD compiler (gen0) and the NEW (gen1) emit the self-image identically at tc1.
#   - cc-route: cc -std=c2x -w -g -pthread -I src/runtime -I src/assert bootstrap/teko.c
#     src/runtime/teko_rt.c src/assert/assert.c -lm -> gen; TEKO_BACKEND=c gen . -o out --no-verify
#     --release -> out/teko.c; sha256(out/teko.c) == sha256(bootstrap/teko.c) == below.
#   - Regression: `examples/regressions/s17_if_region/` (`#if(os=="linux")`/`#elseif`/`#else` — only the
#     matching arm survives; the `#if(os=="windows")` arm calls a NONEXISTENT symbol and is pruned BEFORE
#     type-check without error, proving prune-precedes-checker) exit 42; `s17_arch` exit 64; `s17_composite`
#     (`os=="linux" && arch=="x86_64"`) exit 99; `s17_if_reject` (EXPECT_COMPILE_FAIL) rejects with the
#     expected diagnostics.
# Every capped build (`ulimit -v 6291456`) peaked under 5.8 GB, within the 6 GB cap.
#
# `run: hand-harvest` — no CI run produced this file: reseed-bootstrap.yml is not registered on the
# default branch, so it cannot be workflow_dispatch'd via the API; the harvest is LOCAL on this lane,
# forward-only, and its correctness rests on the self-reproduction above. Mirrors the prior harvests.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (judges on a SWAP).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  20d7cb9b9cda38563d160e8da0cc621b2ecced97431348b92c89fac266fe7539
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (the §17 conditional-compilation parser +
         build-time prune), byte-identical on rebuild across the stable C-route generations (tc1 == tc2 ==
         tc3, the guard-free corpus makes the prune a no-op so no ladder) AND on the cc-route re-emission;
         no unrecovered legacy artifact underlies it.
