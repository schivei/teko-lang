# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): the F3 phantom-owner generic
# factory/method-call fix (checker + monomorph). What this harvest carries:
#   - **F3 fix (compiler — checker + monomorph)**: a static generic factory or method call whose owner
#     type-args include an in-scope ABSTRACT type-param (`Dictionary<StrKey, V>::make()` inside
#     `Map<V>::to_dictionary`, or a free `fn f<V>()`) resolves the owner to a PHANTOM instance
#     (`Dictionary__g__StrKey__V`) that no site stamps and whose `make` is unregistered — it errored
#     `unknown function: make`. Now: `type_phantom_instance_call` (`src/checker/typer.tks`) resolves the
#     factory/method signature against the abstract TEMPLATE and specializes it with the owner
#     substitution the phantom name encodes (`phantom_owner_subst`/`split_phantom_args`/
#     `resolve_phantom_arg_token`, cross-namespace key via `qualified_type_name_by_last`), wired at
#     `type_call` and desugared from `type_method_call` for a phantom-typed receiver; and the mono
#     re-key (`mono_rekey_call_ns`/`mono_rekey_callee_qualifier`, `src/checker/monomorph.tks`) falls an
#     exact `subst_find_inst` miss through to `subst_instance_name` (+ `rekey_phantom_qualifier`),
#     concretizing `Dictionary__g__StrKey__V -> Dictionary__g__StrKey__i64`. Discovery needed NO new
#     enqueue: the concrete instance is auto-found by the existing `stamp_inst_site` body re-scan when
#     the enclosing generic is stamped. This is the static-factory analogue of the self-construct
#     phantom `type_struct_lit` already tolerates; it UNBLOCKS the G6 `Map.to_dictionary` bridge (landed
#     as a LEAF on top of this seed — it uses the factory feature, so it could not be built by the
#     previous `3683d71e` seed).
#
# NOT INERT — BY DESIGN, BUT NO BEHAVIORAL DIVERGENCE. F3 no-ops the compiler's OWN corpus (it has no
# phantom-owner factory/method call), so it changes NO behavior of the self-image. The emitted C differs
# from the previous seed only by the new checker/mono functions + the wiring + mechanical codegen
# temp-counter / source-loc line shifts. The honest statement is "the new compiler reproduces itself
# exactly".
#
# GATE: SELF-REPRODUCE.
#   - C-route chain: gen0 (cc from the PREVIOUS bootstrap `3683d71e`) built the tree → emitted teko.c
#     `3d98db74` (tc1); tc1 -> gen1; gen1 built the tree → tc2 `3d98db74`; tc2 -> gen2; gen2 built the
#     tree → tc3 `3d98db74`. tc1 == tc2 == tc3, byte-identical (sha256 below). F3 never fires on the
#     corpus, so the OLD compiler (gen0) and the NEW compiler (gen1) emit the self-image identically —
#     the stable generation is reached at tc1 and reproduces exactly through tc3. gen0 (F3-incapable)
#     compiles the F3 SOURCE fine because the compiler's own code does not USE the factory-phantom
#     feature; the emitted gen1 IS F3-capable, which is what lets the G6 leaf ride on this seed.
#   - cc-route: cc -std=c2x -w -g -I src/runtime -I src/assert bootstrap/teko.c src/runtime/teko_rt.c
#     src/assert/assert.c -lm -> gen; TEKO_BACKEND=c gen . -o out --no-verify --release -> out/teko.c;
#     sha256(out/teko.c) == sha256(bootstrap/teko.c) == below.
#   - Regression: `examples/regressions/generic_factory_owner_param/` (a free `fn to_dict<V>():
#     Dictionary<StrKey, V>` calling `Dictionary<StrKey, V>::make()` with `V` abstract, instantiated at
#     `i64` and `str`) builds+runs green (exit 47) — the exact shape that errored `unknown function:
#     make` before. `examples/regressions/map_to_dictionary/` (the G6 leaf on top) builds+runs green
#     (exit 77).
# Every capped build (`ulimit -v 6291456`) peaked under 5.4 GB, within the 6 GB cap.
#
# `run: hand-harvest` — no CI run produced this file: reseed-bootstrap.yml is not registered on the
# default branch, so it cannot be workflow_dispatch'd via the API; the harvest is LOCAL on this lane,
# forward-only, and its correctness rests on the self-reproduction above. Mirrors the prior harvests.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (judges on a SWAP).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  3d98db743cf05f9a79bc3fd666879c7e131bcb7abdd00e72de003ed90a30ea90
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (the F3 phantom-owner generic
         factory/method-call checker+monomorph fix), byte-identical on rebuild across the stable
         C-route generations (tc1 == tc2 == tc3) AND on the cc-route re-emission; no unrecovered legacy
         artifact underlies it. The G6 `Map.to_dictionary` bridge rides on top as a LEAF (not in this
         seed; the F3-capable seed compiler builds it).
