# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on feat/arrays-reclaim-first, forward-only): refreshes the seed for
# marco 1 of the io-streaming / array-reclaim campaign — additive compiler surface for the
# file-stream layer: the Linux/macOS syscall constants for open/read/write/lseek/close/statx in
# src/sys/sys.tks and the `teko::mem::byte_ptr(xs, i)` builtin (the address of a `[]byte` element,
# sibling of `buf_ptr`) in the checker/codegen. No src site calls byte_ptr yet, so the emitted C
# for every program is unchanged from the prior seed except for the compiler's own new branches.
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (gen2 == gen3), backend C, CC=clang.
#   - gen1 (linked from this bootstrap via clang) builds gen2; gen2 builds gen3; gen2.c == gen3.c
#     byte-identical, and this file is that stable gen2.c.
#   - Guard: builds ran under `ulimit -v 10485760` (10 GiB dev allowance for the in-progress
#     campaign) with TEKO_BACKEND=c; gen2 peak VmHWM ~6.46 GiB (reduction lands with the codegen
#     -> FileStream + push expurgo + arena reclaim crumbs still ahead).
#
# `run: hand-harvest` — no CI run produced this file: the harvest is LOCAL on this lane,
# forward-only, its correctness resting on the clean fixpoint above.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (DISABLED
# per CLAUDE.md — the seed is accepted by force; these comments do not gate).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  f54f1dcbf4384081a3ffc5677232953fc0bf65fae7dec50ce184f7e44f91229d
since:   0.3.1.0
legacy:  none — self-reproducible harvest of marco 1 (syscall constants + byte_ptr builtin);
         byte-identical on rebuild (clean fixpoint gen2 == gen3, gen2.c == gen3.c, 23 230 930 bytes).
