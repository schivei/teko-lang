# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on feat/arrays-reclaim-first, forward-only): refreshes the seed for
# marco 3 of the io-streaming / array-reclaim campaign — the TOTAL forms now ride the stream layer:
# src/io/io.tks read_file/write_file/write_file_bytes/append_file drop `extern ... from "teko_rt"`
# and call read_stream/write_stream/append_stream (raw syscalls). Signatures are unchanged, so
# every caller is untouched; because they are now real Teko functions (not extern) the codegen's
# host-FFI shortcut no longer fires for them.
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (gen2 == gen3), backend C, CC=clang.
#   - Write exactness: the teko.c a stream-based compiler WRITES is byte-identical to the one the
#     prior extern-write compiler wrote (proven cmp of the two).
#   - Fixpoint: gen2 builds gen3, gen2.c == gen3.c byte-identical; this file is that stable output.
#   - Guard: builds ran under `ulimit -v 10485760` (10 GiB dev allowance for the in-progress
#     campaign) with TEKO_BACKEND=c; gen2 peak VmHWM ~6.44 GiB (reduction lands once the codegen
#     emits straight into a FileStream and the push expurgo + arena reclaim land).
#
# `run: hand-harvest` — no CI run produced this file: the harvest is LOCAL on this lane,
# forward-only, its correctness resting on the clean fixpoint above.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (DISABLED
# per CLAUDE.md — the seed is accepted by force; these comments do not gate).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  c78d791906d4f74723ab652484955dc5f1e8438297b0d7fcc87ed33254343590
since:   0.3.1.0
legacy:  none — self-reproducible harvest of marco 3 (TOTAL read/write forms over the stream layer,
         plus dead-const cleanup in file_stream.tks); byte-identical on rebuild (clean fixpoint
         gen2 == gen3, gen2.c == gen3.c, 23 246 159 bytes).
