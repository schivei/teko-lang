# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): the FINAL reseed of the
# tree-wide cleanup lane. The prior seed (502a5ea7) was the OLD compiler — its codegen predates
# the current front-end, so the published release cannot parse this tree and falls back to this
# bootstrap (expected). This harvest is the first bootstrap emitted by the NEW compiler: it carries
# the tree-wide canonicalization AND the #os cross-emit at once.
#
# WHAT THIS HARVEST CARRIES (the delta the new codegen bakes into the emitted C):
#   - tree-wide cleanup / W15 canonicalization across all leaf `.tks` (doc-comments-only,
#     flatten/extract, unique tree-wide helper names) — the message/line shifts that force a reseed.
#   - #os CROSS-EMIT: the ONE emitted `teko.c` carries every target at once via C `#if` — the
#     macOS `__APPLE__` libSystem arm (os_sync_wait_on_address / mmap FFI), the Windows `_WIN32`
#     arm (WaitOnAddress / VirtualAlloc), and the Linux raw-syscall arm, plus the arch ladder
#     (`__x86_64__` / `__aarch64__`) — so the same bytes cross-compile to every (os, arch).
#   - DocSpan: doc-comment span tracking in the front-end.
#   - removal of the self-exercised tests (the self-build/fixpoint is that proof).
#   - removal of `/* */` block comments (not doc comments — owner ruling).
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (gen2 == gen3), backend C.
#   - C-route chain: gen0 = cc(prior 502a5ea7 seed) built gen1 = gen0(src) (gen1.c = OLD-algorithm
#     re-emission, deliberately NOT the harvest); gen1's OWN (new) algorithm then emitted
#     gen2 = gen1(src) and gen3 = gen2(src). Fixpoint verdict PASSED: gen2 == gen3 byte for byte
#     AND gen2.c == gen3.c byte for byte (22 827 971 bytes). This harvested teko.c IS that stable
#     emission (== gen2.c == gen3.c).
#   - Cross-emit present in this file: `#if defined(__APPLE__)` / `#if defined(_WIN32)` arms with
#     os_sync_wait_on_address + mmap (macOS), WaitOnAddress + VirtualAlloc (Windows), and the
#     `__x86_64__` / `__aarch64__` SYS_* ladder for the Linux arm.
#   - AUTO-REPRODUCTION: cc(this bootstrap) rebuilt to a compiler that re-emits this same
#     bootstrap/teko.c byte-identical.
#   - Guard: every build ran under `ulimit -v 6815744` (6.5 GiB) with TEKO_BACKEND=c; the
#     self-build peak fit (VmPeak ~6.30 GB virtual, VmHWM ~5.95 GB resident).
#
# `run: hand-harvest` — no CI run produced this file: the harvest is LOCAL on this lane,
# forward-only, its correctness resting on the clean fixpoint and the cross-emit above.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (DISABLED
# per CLAUDE.md — the seed is accepted by force; these comments do not gate).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  462178b3c01e16b55f93b3a80bd0042c8c844eb39e3cd9a007dcf8744c87fecd
since:   0.3.1.0
legacy:  none — self-reproducible harvest: tree-wide cleanup + #os cross-emit + DocSpan + test
         removal + block-comment removal; byte-identical on rebuild (clean fixpoint gen2 == gen3,
         gen2.c == gen3.c, 22 827 971 bytes).
