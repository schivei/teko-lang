# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on feat/arrays-reclaim-first, forward-only): marco 7 of the
# io-streaming / array-reclaim campaign — fourth memory-reduction batch (diminishing returns; the
# remaining root bytes are increasingly LIVE AST/type data held through compilation, reachable only
# by arena-per-scope reclaim, not by push conversion). Converts the last easy plain-push->root MAP
# / FILTER sites the arena map still blamed:
#   - checker::type_table_of (FILTER: count TypeDecls, one preallocated regs array; the tt_push loop
#     was plain root push) + count_type_decls helper.
#   - checker::canon_ref_list (MAP).
#   - checker::inline_rw_exprs (MAP []TExpr -> []TExpr).
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (gen2 == gen3), backend C, CC=clang.
#   - gen2 builds gen3; gen2.c == gen3.c byte-identical; this file is that stable output.
#   - MEMORY: gen2 peak VmHWM 3.38 -> 3.32 GiB (VmRSS 3.22 -> 3.16 GiB); cumulative from the
#     campaign start 6.46 -> 3.32 GiB (VmRSS 6.44 -> 3.16). Guard: builds ran under
#     `ulimit -v 10485760` (10 GiB dev allowance) with TEKO_BACKEND=c.
#
# `run: hand-harvest` — LOCAL on this lane, forward-only, resting on the clean fixpoint above.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (DISABLED
# per CLAUDE.md — the seed is accepted by force; these comments do not gate).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  8b1c6996b140ed0e8f2063db6bf19bed89b15f3a4a731dcbb4c0d351ee38a728
since:   0.3.1.0
legacy:  none — self-reproducible harvest of marco 7 (type_table_of/canon_ref_list/inline_rw_exprs
         push->preallocate, gen2 peak 3.38 -> 3.32 GiB); byte-identical on rebuild (clean fixpoint
         gen2 == gen3, gen2.c == gen3.c, 23 256 062 bytes).
