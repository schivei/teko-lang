# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on feat/arrays-c3-codegen-buffer, forward-only): refreshes the seed
# for crumb C3 of the array-expurgo — the codegen emission buffer is converted from the
# append_fo/push_fo copy-grow primitives to a chunked builder (Cb) that appends by index and
# concatenates once into a single [total]byte, with zero dynamic array growth on the append path.
# The prior seed (sha256 1b716bf9…, 23 154 060 bytes) predated this codegen change, so it no
# longer reproduces this tree's own output; this harvest is the first bootstrap emitted by the
# compiler that carries the C3 builder.
#
# WHAT THIS HARVEST CARRIES (the delta the new codegen bakes):
#   - EMISSION BUFFER: cb/cb_str/cb_byte and cg_format_c/ffi_export build into a Cb chunked
#     builder (inline c0/c1 sealed slots + lazily-grown prev array + current chunk) instead of
#     tk_slice_push_fo/tk_slice_push_r. The builder tracks a running .len equal to the old flat
#     buffer's byte count, so every gensym derived from buf.len is unchanged.
#   - OUTPUT INVARIANT: the emitted C is byte-identical to the previous codegen for every program
#     (the prior seed's gen1 and this seed both emit the same bytes), so the change is a pure
#     runtime-representation swap of the emitter, not an output change.
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (gen2 == gen3), backend C, CC=clang.
#   - AUTO-REPRODUCTION: clang(this bootstrap) = gen0'; gen0'(src) = gen1' re-emits this same
#     bootstrap/teko.c byte-identical (sha256 below); gen2' = gen1'(src) and gen3' = gen2'(src) are
#     byte-identical to each other and their emitted C equals this file.
#   - Guard: every build ran under `ulimit -v 6815744` (6.5 GiB) with TEKO_BACKEND=c.
#
# `run: hand-harvest` — no CI run produced this file: the harvest is LOCAL on this lane,
# forward-only, its correctness resting on the clean fixpoint above.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (DISABLED
# per CLAUDE.md — the seed is accepted by force; these comments do not gate).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  edebe8d9d1a0b0e31c35e33844e35b4118c274afa4f30c3404617dd49c5e0c43
since:   0.3.1.0
legacy:  none — self-reproducible harvest of C3 (codegen emission buffer -> chunked builder);
         byte-identical on rebuild (clean fixpoint gen2 == gen3, gen2.c == gen3.c, 23 211 269 bytes).
