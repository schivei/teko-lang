# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): re-harvest after implementing the
# standing owner law (CLAUDE.md "Convenções da linguagem/codebase", commits 03e09131 + 18d63a54,
# owner 2026-08-18) that had been RECORDED but never landed — `let`/`mut` do not exist, only
# `var`/`const`. `TokenKind::Let`/`Mut` (never produced by the lexer) and `BindKind::Let`/`Mut`
# (parser::ast.tks) are removed; `BindKind` collapses to `enum { Var; Const }` (`Mut`→`Var`,
# `Let`→`Var`, mechanically, per "sem repensar Let — discard/loop-var idem"). The now-dead
# case-impossible branches this exposed are removed rather than reworded: the unreachable `let
# ref`/`mut ref` tripartite check inside `parse_binding` (src/parser/parse_stmt.tks — structurally
# unreachable given `is_var_binding_head`'s own gating, confirmed by trace), the dead
# `TokenKind::Let`/`Mut` arms of `is_variable_binding_head` (src/parser/parse_decl.tks), the dead
# `Let`-arm of `lp_is_const_binding` (src/checker/comptime_fold.tks), and the dead `let`/`mut`
# entries in the fmt keyword table and the LSP reserved-keyword list. This is a COMPILER change to
# the corpus the self-build exercises, so it alters the emitted teko.c and the seed is re-harvested
# per CLAUDE.md ("Reseed de bootstrap/teko.c quando mudança de compilador altera o C emitido: harvest
# local (fixpoint gen2==gen3), sem gate de provenance").
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (gen2 == gen3), backend C.
#   - gen1 (built from the PRIOR bootstrap/teko.c, sha256 b3a45b4478dea0ef9e9f4a7f2bbfd0ae53fec4b5
#     3da8343b5ff75de6b38d27f2) builds gen2, gen2 builds gen3; gen2 == gen3 byte-identical (binary
#     AND source), gen2.c == gen3.c byte-identical (23 564 696 bytes) — this file IS that stable
#     output — the compiler built from this seed rebuilds the tip and re-emits this seed byte for byte.
#   - MEMORY: gen2/gen3 C-route build peak ~3.28 GiB (TEKO_BACKEND=c, TEKO_CC=clang, --release).
#
# `run: hand-harvest` — LOCAL on this lane, forward-only, resting on the clean fixpoint above.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (DISABLED
# per CLAUDE.md — the seed is accepted by force; these comments do not gate).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  de19ebe0d3e71f5d09a4606bac384d72cc15bf640b7cee8f24b3c7e222febcbd
since:   0.3.1.0
legacy:  b3a45b4478dea0ef9e9f4a7f2bbfd0ae53fec4b53da8343b5ff75de6b38d27f2 — self-reproducible harvest
         of the let/mut-retirement BindKind collapse (Var/Const only); byte-identical on rebuild
         (clean fixpoint gen2 == gen3, gen2.c == gen3.c, 23 564 696 bytes).
