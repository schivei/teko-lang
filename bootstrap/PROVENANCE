# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, by-hand per owner order): re-harvested after TEACHING the C#-style delegate
# TYPE spellings `func<…>` / `action<…>` and function-without-return (0.3.1 §2 surface). This is the
# BRIDGE reseed: the compiler's OWN source still uses the old `(A, B): R` / `: void` forms, so the
# previous `508514…` bootstrap can still build the tree; this new seed additionally UNDERSTANDS
# `func`/`action` + optional return, so the NEXT commit can convert every usage to the new spelling
# and retire the old forms. The owner did NOT edit bootstrap/teko.c itself; this reseed only realigns
# the seed with the current source.
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh, which
# only ever judges this record on a SWAP (an add, replace, or remove-then-reintroduce of
# bootstrap/teko.c) — never for the unchanged incumbent this record describes.
#
# teach func/action delegate types + function-without-return (bridge)
# ---------------------------------------------------------------------------------------------
# SURFACE RESPELLING, purely additive on this bridge commit. What SAILED:
#   - PARSER (`parse_type.tks`): `func<T1, …, Tn, R>` / `action<T1, …, Tn>` — the C# `Func`/`Action`
#     delegate TYPE spellings. `func`'s LAST type arg is the return, the rest are params (`func<R>` =
#     0 params returning R); `action` has NO return (unit), all args are params, and BARE `action`
#     (no `<…>`) is the zero-parameter no-return function type. BARE `func` and empty `func<>`/`action<>`
#     are rejected. Arity capped at 16 type args (the general generic cap). Both DESUGAR into the
#     existing `parser::FunctionType` AST node, so checker / codegen / `.tkb` are UNCHANGED — codegen
#     already lowers every `FunctionType` to the opaque `tk_closure` fat pointer. The no-return case
#     carries an UNSPELLABLE internal sentinel `()` in the shared `ret` slot.
#   - CHECKER (`scope.tks` builtin_type): the `()` sentinel resolves to the internal `Void{}` (the
#     SAME unit the old `(A, B): void` produced) — added alongside the still-live `void` recognition.
#   - function-without-return already worked (a `fn` with no `: R` sets `has_return = false` → `Void`).
#
# NO new AST node, NO `.tkb` wire change (`FunctionType.ret` stays a mandatory TypeExpr — the `()`
# sentinel is an ordinary named type that all existing walks recurse over and `builtin_type` maps to
# `Void`), NO runtime C touched. The old `(A, B): R` / surface `void` forms are STILL accepted on this
# bridge — they are retired in the NEXT commit, which the bridge builds.
#
# FIXPOINT. gen0 (cc of the previous `508514…` bootstrap) C-built the taught source into bin-a;
# compiling bin-a/teko.c with cc into the new compiler and rebuilding produced a byte-identical
# bin-b/teko.c (sha256 below). The committed file is that fixpoint — it self-reproduces:
#   cc(bootstrap/teko.c) -> gen0;  TEKO_BACKEND=c gen0 build . -> bin-a;  cc(bin-a/teko.c) -> bin;
#   TEKO_BACKEND=c bin build . -> bin-b;  sha256(bin-a/teko.c) == sha256(bin-b/teko.c).
#
# `run: hand-harvest` — no CI run produced this file; it was harvested by hand on the branch, in the
# owner's ordered sequence. `commit: pending drain` — the landing commit is not yet known at harvest
# time (this record is written in the same change that commits the file).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  6857530c475dcef582f27cf8bb1acdf7fce77c18de0eb26e9ae22ad9b306214d
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (teach func/action delegate types +
         function-without-return, bridge); sha256 is read directly off the committed file and was
         proven a fixpoint (bin-a == bin-b, byte-identical).
