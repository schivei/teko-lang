# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local, forward-only): DOC-AS-SPAN memory optimization. Doc-comments are no
# longer materialized as `str` through the AST/TAST; a dedicated `parser::DocSpan`
# `{ file_id, byte_offset, byte_len }` carries the span and re-reads the text on demand at the sole
# borders that need it (`.tkh` header emission, LSP hover). The lexer stamps each token with its
# absolute byte offset and file (`tokenize_file`); the parser records the span; the `.tkb` codec no
# longer serializes doc text at all. This is a compiler change (the emitted `teko.c` moved), so a
# reseed is required.
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (gen2 == gen3).
#   - C-route chain: gen0 (cc of the prior 502a5ea7 seed) built gen1 = gen0(src); gen1's OWN
#     algorithm (this tree's) then emitted gen2 = gen1(src) and gen3 = gen2(src). The fixpoint
#     holds: gen2.c == gen3.c byte for byte (22824687 bytes). This harvested teko.c IS that stable
#     emission (== gen2.c == gen3.c).
#   - The `#arch` cross-emit arch ladder is preserved (`#if …__x86_64__… SYS_MMAP 9ULL /
#     #elif …__aarch64__… SYS_MMAP 222ULL / #else #error`, all 11 SYS_* families) — this pass did
#     not touch the cross-emit path.
#   - `.tkh` byte-equivalence: a library built by the OLD compiler and by the NEW compiler emits the
#     identical `.tkh` (doc text preserved via `DocSpan.read()`); the `.tkb` no longer carries doc.
#   - `TEKO_MEM_PARANOID=1` full compiler self-build on the Teko arena exits 0. Self-reported peak
#     5938.9 MB under paranoid (~5751.0 MB plain).
#
# `run: hand-harvest` — no CI run produced this file: the harvest is LOCAL on this lane, forward-only,
# its correctness resting on the self-reproduction and the gates above. Mirrors the prior harvests.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (judges on a SWAP).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  499465e773921aa5d280376fe070aae71263577639a39bee91f9b9d0caac08da
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (doc-as-DocSpan memory optimization),
         byte-identical on rebuild across the Teko-arena C-route generations (clean fixpoint
         gen2 == gen3, gen2.c == gen3.c); no unrecovered legacy artifact.
