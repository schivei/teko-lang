# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): the §11.2 "EMISSÃO LIMPA" step 1 —
# the `--explicit` STRONG-TYPING GATE lands in the compiler (checker), so the compiler's own emitted
# teko.c gains the new gate logic. What this harvest carries:
#   - **`--explicit` flag (compiler — checker + CLI)**: a new `Env.explicit: bool` threaded from the
#     program root through the checker's inference/coercion points. Under `--explicit` (DEFAULT OFF,
#     the checker never bars without it — inference stays a valid language feature for Teko users):
#       * `explicit_binding_gate` in `type_binding` — a `var`/`const` binding whose target is a named
#         `SimpleName` (never `_`, never a destructure/alias) with `!has_type` is an ERROR. Loop-head
#         preamble is typed with the flag CLEARED (iteration vars never gated); match `as` aliases and
#         discard `_` pass; return needs no gate (Teko never infers a named-fn return — absence of
#         `→ T` is a complete void signature; `const`/params/fields are already mandatory in parser).
#       * `explicit_cast_gate` (replaces the retired `warn_redundant_cast`) at its 3 sites — a
#         provably-unnecessary conversion (same-type / redundant lossless-widen) is an ERROR under
#         `--explicit` and SILENT in the default build (the historical stderr warning is gone; the
#         `TCast` node is still kept so the D4 arith-cast-ratio ceiling still counts it).
#   - Touch points: `scope.tks` (Env + `with_explicit` + 9 constructors), `typer.tks` (the two gates
#     + `type_loop` flag-clear + `type_program_with_deps_pre_mono`), `project.tks` (`explicit_of`,
#     `--explicit` parse + threading), `main.tks`, `lsp_api.tks`, `checker_test.tkt`.
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (tc1 == tc2 == tc3, NOT a one-step ladder).
#   - The gate is DEFAULT-OFF and the removed warnings are stderr-only, so the emitted bytes of the
#     WHOLE TREE are the same whether emitted by gen0 (old seed logic) or gen1 (new logic): the seed
#     `49e5a64c` already converges on the new image in ONE pass, with no transitional preamble.
#   - C-route chain: gen0 (cc, `-pthread`, from bootstrap `49e5a64c`) built the tree → tc1
#     `bea94859…`; tc1 -> gen1; gen1 built the tree → tc2 `bea94859…`; tc2 -> gen2; gen2 built the
#     tree → tc3 `bea94859…`. tc1 == tc2 == tc3, byte-identical (sha256 below).
#   - cc-route: cc -std=c2x -w -O1 -pthread -I src/runtime -I src/assert bootstrap/teko.c
#     src/runtime/teko_rt.c src/assert/assert.c -lm -> gen; TEKO_BACKEND=c gen . -o out --no-verify
#     --release -> out/teko.c; sha256(out/teko.c) == sha256(bootstrap/teko.c) == below.
#   - MEM_PARANOID: `TEKO_BACKEND=c TEKO_MEM_PARANOID=1 gen1 . -o out --no-verify --release` exits 0
#     (whole-tree). Self-reported peak ~5.8 GB (within the 6 GB cap).
#   - Default build (no `--explicit`) is byte-identical to the pre-flag behavior; gen1 emitted ZERO
#     redundant-cast warnings where gen0 printed 15 (item-C default-silence confirmed).
#
# `run: hand-harvest` — no CI run produced this file: reseed-bootstrap.yml is not registered on the
# default branch, so it cannot be workflow_dispatch'd via the API; the harvest is LOCAL on this lane,
# forward-only, and its correctness rests on the self-reproduction above. Mirrors the prior harvests.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (judges on a SWAP).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  bea94859848256e1a8f64c9cf33b9040bd49dbc41abd2c3b3693c091c75bfb72
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (the §11.2 EMISSÃO LIMPA step 1: the
         `--explicit` strong-typing gate + the redundant-cast gate, both DEFAULT OFF), byte-identical
         on rebuild across the stable C-route generations (clean fixpoint tc1 == tc2 == tc3) AND on
         the cc-route re-emission; no unrecovered legacy artifact.
