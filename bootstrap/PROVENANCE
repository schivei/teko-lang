# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on feat/section7-di-service, forward-only): the §7 Part-A BACKEND SEAM
# `svc_scope_expr` — the point where a service's lifetime finally reaches the arena slot. §7's surface
# had already landed and reseeded (40778f95); this harvest carries the seam that makes a `singleton`
# actually a singleton (one materialization cached in the task root) and leaves `Transient` building
# fresh. Doc 1 §8 swaps two lines of that one function (task root -> THREAD root, scoped -> thread
# sub-root) and nothing else moves — not the signature, not `svc_type_id`, not the typer, not a fixture.
#
# GATE (unchanged in kind from the item-14 reseed): SELF-REPRODUCE.
#   - C-route chain s1 == s2 == s3, byte-identical (sha 3f5868ae…): the seam is INERT on the compiler's
#     own corpus, which declares no services, so the fixpoint never moved.
#   - cc-route: cc bootstrap/teko.c -> gen; TEKO_BACKEND=c gen build . -> teko.c;
#     sha256(teko.c) == sha256(bootstrap/teko.c) == below. Verified this run.
#   - Behaviour: a `singleton` claimed twice shares ONE instance (12) while a `transient` builds fresh
#     (11) — the behaviour that did not exist before the seam. `service_svc` 9/9 ACCEPT scenarios pass;
#     `svc_reject` still rejects.
# Every capped build (`ulimit -v 6291456`) peaked ~4.25 GB, well under the 6 GB cap.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh, which only
# ever judges this record on a SWAP.
#
# ITEM-14 FAT VALUE STRUCT reseed
# ---------------------------------------------------------------------------------------------
# WHAT ENTERS (committed to `src/` on feat/item14-fat-header in this same lane):
#   - runtime C (`teko_rt.h`, the maintained C exception): `tk_struct_hdr` — ONE `uintptr_t`
#     self-pointer word, deliberately smaller than an object header (no vtable, no identity region),
#     plus `tk_struct_hdr_of` / `tk_struct_hdr_remat` (materialize at construction / re-materialize
#     after a copy).
#   - codegen (`codegen.tks`): `tk_struct_hdr __hdr` is the FIRST member of every `tk_t_<mangle>`
#     value-struct typedef; and a value-struct instance method now takes its receiver as that HEADER
#     POINTER (`tk_t_X *self`), so a write through `self` STICKS on the caller's materialization. Three
#     coordinated sites — the signature, every `self` use (which emits `(*self)`, so `self.x` writes
#     through while passing/returning `self` still yields a COPY), and the call site's auto-`&` (spilling
#     a non-addressable receiver to a temporary first). A CLASS is excluded throughout: it is already a
#     pointer here and owns an arena, exactly as §13.2 seals it.
#   - LIR (`lower.tks`, `lir.tks`): the native layout seeds the same header slot at offset 0 behind a
#     `with_hdr` parameter, so every declared field shifts by exactly one word in BOTH backends;
#     `struct_hdr_field_name()` is the single spelling the two share, so offsets agree by construction.
#   Carried forward: the completed §9.D sweep, §9.2b shape-constraint solver, §9 value-type operators,
#   and the item-14 checker crumbs 1-4 (readonly markers/enforcement, mutating-method inference,
#   immutable-receiver check, value-type readonly self) that landed before this lane.
#
# NOT INERT — BY DESIGN. Every struct in the compiler's own source is now fat, so the emitted C differs
# from the previous seed's throughout. The honest statement is not "nothing changed" but "the new
# representation reproduces itself".
#
# GATE: SELF-REPRODUCE (plan §11.3, and the coordinator's standing ruling for this crumb).
#   - C-route chain: n2 == n3, byte-identical binaries (sha b689e367…) and byte-identical emitted C
#     (the artifact committed here). The representation is a FIXPOINT: a fat-emitting compiler compiled
#     by a fat-emitting compiler reproduces itself.
#   - cc-route: cc -std=c2x bootstrap/teko.c -> gen; TEKO_BACKEND=c gen build . -> teko.c;
#     sha256(teko.c) == sha256(bootstrap/teko.c) == below. Verified this run.
#   - Behaviour, verified with the FRESHLY cc-compiled seed rather than the build that produced it:
#     `examples/regressions/value_struct_mut` builds and all four scenarios pass (the mutation sticks,
#     the copy stays independent, the encapsulated setter lands, `ref` replaces a whole slot), and
#     `value_struct_mut_reject` still rejects. `value_type_operators` passes all six scenarios.
#   - Byte-identity with the PREVIOUS seed is deliberately NOT claimed and is not the gate; the native
#     `gen2 == gen3` at the fat representation is the marker that closes the backend/arena wave (§5/§11).
# Every capped build (`ulimit -v 6291456`) peaked ~4.25 GB, well under the 6 GB cap.
#
# `run: hand-harvest` — no CI run produced this file: the canonical reseed-bootstrap.yml flow seeds from
# the RELEASED binary, which predates the 0.3.1 surface AND §14, so the harvest is LOCAL on this lane,
# forward-only, and its correctness rests on the self-reproduction above rather than a CI run-id.
# `commit: pending drain` — the landing commit is written in this same change.
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  bb9a92c57da65b18f2afdf08683246d5a4ba49aa0a51e1649ca9fd8b63fea109
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (item-14 fat value struct + `self`-as-ref
         receiver ABI, on top of the completed §9.D sweep + §9.2b + §9 value-type operators + the item-14
         checker crumbs), byte-identical on rebuild across the C-route generations AND on the cc-route
         re-emission; no unrecovered legacy artifact underlies it.
