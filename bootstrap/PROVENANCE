# bootstrap/PROVENANCE — provenance for the bootstrap/teko.c currently committed to this tree.
#
# RESEED (hand-harvest, local on fix/retirement, forward-only): §16 ARENA CRUMB D — the Teko arena
# provider's real current-region stack (`docs/design/plano-s16-runtime-prelude-injection.md` §3), the
# prerequisite for the L2 flip. What this harvest carries:
#   - **arena current-region stack (compiler-shipped runtime — `src/runtime/arena.tks`)**: the three
#     missing provider fns and a real `region_current`, backed by a per-flow bounded stack in the
#     arena's OWN mmap'd CONTROL block, mirroring the C twin (`teko_rt.c` `tk_region_enter`/
#     `tk_region_leave`/`tk_region_current`/`tk_alloc`, `cur_regions[64]`/`cur_rsp`):
#       * CONTROL layout additions APPENDED after the free bins: `CTRL_CUR_SP` (the stack pointer,
#         the C twin's `cur_rsp`), `CTRL_CUR_STACK` (64 region handles, the C twin's `cur_regions`),
#         `CUR_STACK_SLOTS = 64`, and `CTRL_BYTES` grown 33920 -> 34448 (the scalars + mark stack +
#         free bins + the 64-slot current-region stack, alignment-rounded). The mapping is kernel
#         zero-filled, so `CTRL_CUR_SP` starts at 0 = "root is current" with no init write.
#       * `ar_cur_slot`/`ar_cur_current`/`ar_cur_enter`/`ar_cur_leave` — the allocation-free stack
#         primitives. `ar_cur_current` reads the TOP (guarded `sp` in `(0, CUR_STACK_SLOTS]`, a
#         non-zero slot) and falls to the root otherwise; `ar_cur_enter` writes the slot under the cap
#         and increments unconditionally so an over-deep push still balances a later leave;
#         `ar_cur_leave` decrements only when non-zero — byte-for-byte the C twin's discipline.
#       * `region_enter`/`region_leave`/`alloc` (the three missing `CgArenaSym` kinds) + the real
#         `region_current` replacing the root-only stub. `alloc` routes through
#         `ar_region_alloc_w(control, ar_cur_current(control), n)` (the allocation-free dialect); the
#         obs-histogram side effect of the C `tk_alloc` is diagnostic-only and deliberately NOT
#         reproduced (the arena core forbids the computed-string path it would need).
#   - **DORMANT after crumb D**: `cg_arena_sym` is UNFLIPPED, so the ~197 allocation call sites still
#     route to the C arena; the self-image emits the new functions but RUNS on the C arena. Crumb D is
#     additive and behavior-neutral for the self-build — the temp-id shift of the three new fns is the
#     only emitted-C delta (ACHADO-A), so gen0 (cc of the prior seed) and gen1 emit the same whole-tree
#     bytes: a one-pass convergence.
#   - Touch points: `src/runtime/arena.tks` (+ the RF5 promoted-probe fixture
#     `examples/regressions/current_region_stack`, a leaf, not in the self-image).
#
# GATE: SELF-REPRODUCE — CLEAN FIXPOINT (tc1 == tc2 == tc3).
#   - The current-region stack is behavior-neutral for the self-build (the arena stays dormant), so
#     gen0 and gen1 emit byte-identical whole-tree C (sha256 below).
#   - C-route chain: gen0 (cc, `-pthread`, from the prior bootstrap) built the tree -> gen1; gen1's
#     emitted C == gen0's emitted C; the C-route fixpoint gate returns PASSED (gen2 == gen3 byte for
#     byte, gen2.c == gen3.c, 22668287 bytes). `TEKO_MEM_PARANOID=1` full compiler build exits 0 (the
#     memory keystone — the C arena's poison path stays clean, and the paranoid-build emitted C is
#     byte-identical to the plain build). RF5 `current_region_stack` (enter-a-child /
#     alloc-lands-in-child / leave / alloc-lands-in-root / drop-the-child, over the raw mmap fundo)
#     exits 42 on the C leg. Self-reported peak 5742.1 MB (within the 6 GB cap).
#
# `run: hand-harvest` — no CI run produced this file: reseed-bootstrap.yml is not registered on the
# default branch, so it cannot be workflow_dispatch'd via the API; the harvest is LOCAL on this lane,
# forward-only, and its correctness rests on the self-reproduction above. Mirrors the prior harvests.
# `commit: pending drain` — the landing commit is written in this same change.
#
# Format and parser: scripts/degrau.sh's kv_field; enforced by scripts/provenance_gate.sh (judges on a SWAP).
c:       bootstrap/teko.c
run:     hand-harvest
commit:  pending drain
sha256:  49c6df6e521cb55e852c1e2af1cfe846bc5404d7416edf1580643d28e0b95cdb
since:   0.3.1.0
legacy:  none — this is an exact, self-reproducing hand harvest (the §16 arena crumb D current-region
         stack — additive, behavior-neutral while the Teko arena stays dormant), byte-identical on
         rebuild across the stable C-route generations (clean fixpoint tc1 == tc2 == tc3); no
         unrecovered legacy artifact.
