/**
 * Interface representing an iterator for a collection.
 *
 * @template T The type of the elements returned by the iterator.
 */
exp type Iterator<T> = interface {
    /**
     * Checks if the iterator has more elements.
     *
     * @return `true` if the iterator has more elements, `false` otherwise.
     */
    pub fn has_next(): bool
    /**
     * Returns the next element in the iteration.
     *
     * @return The next element in the iteration.
     */
    pub fn next(): T
    /**
     * Returns the next element in the iteration without advancing the iterator.
     *
     * @return The next element in the iteration.
     */
    pub fn peek(): T
    /**
     * Resets the iterator to the initial position.
     */
    pub fn reset()
    /**
     * Creates a copy of the iterator.
     *
     * @return A new iterator that is a copy of the current iterator.
     */
    pub fn clone(): Iterator<T>
}