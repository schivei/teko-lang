namespace teko

/**
 * A character type represented as a 32-bit unsigned integer.
 */
exp global type char = u32 {
    /**
     * Encodes the character into a byte array using the specified encoding.
     */
    pub fn encode(enc: Encoding = Encoding::UTF8): []byte {
        enc.bytes(self to u32)
    }
}
