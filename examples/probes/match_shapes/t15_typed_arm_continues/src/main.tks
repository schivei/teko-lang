fn main() {
    let a: u64 | null = 42
    mut acc: u64 = 0
    mut i: u64 = 0
    loop {
        if i >= 3 { break }
        i++
        let r: u64 = match a { u64 as v => v; null => continue }
        acc = acc + r
    }
    exit(acc)
}
