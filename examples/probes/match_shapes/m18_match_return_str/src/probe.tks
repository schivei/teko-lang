fn pick(a: u64 | null, fallback: str): str {
    match a { u64 as _v => "Woof"; null => fallback }
}
pub fn probe(): u64 {
    var r1 = pick(1, "?")
    var r2 = pick(null, "?")
    if r1 == "Woof" && r2 == "?" { 42 to u64 } else { 1 to u64 }
}
