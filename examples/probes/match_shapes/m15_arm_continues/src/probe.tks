pub fn probe(): u64 {
    var a: u64 | null = 42
    var acc: u64 = 0
    var i: u64 = 0
    loop {
        if i >= 3 { break }
        i++
        var r = match a { u64 as v => v; null => continue }
        acc = acc + r
    }
    acc
}
