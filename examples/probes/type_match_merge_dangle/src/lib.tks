type A = struct { x: i64 }

type B = struct { y: i64 }

type Thing = variant A | B

fn pick(n: i64): Thing {
    match n {
        0 => A { x = 1 }
        _ => B { y = 2 }
    }
}

fn read(t: Thing): i64 {
    match t {
        A as a => a.x
        B as b => b.y
    }
}
