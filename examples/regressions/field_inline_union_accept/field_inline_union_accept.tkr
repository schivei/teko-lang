# field_inline_union_accept.tkr — the 9D-T1 (crumb 0019) field-position-union anchor. A struct
# field typed `A | B | C` is a structural union — the existing var/param/return union checking
# reused for the field slot. `Holder { x = B { b = 7 } }` stores the middle branch; matching
# `h.x` proves the value IS one of the branches. `type X = variant` still lives untouched.

Feature: field_inline_union_accept — a struct field accepts an inline `A | B | C` union

  Scenario: field_holds_and_matches_one_branch
    When the program is built and run
    Then it exits 0
