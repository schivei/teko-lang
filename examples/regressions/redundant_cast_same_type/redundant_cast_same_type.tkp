# Regression (cast-width-hygiene-0.3.1.md D1, C4 "varre -> liga"): `x to u32` where `x` is
# already `u32` is a provable no-op — the D1 diagnostic rejects it as a direct compile ERROR
# (no warn phase, owner ruling 2026-07-24). Proves the class-1 rule is live once C4 flips it on.
#
#   teko build examples/regressions/redundant_cast_same_type   # -> compile error (EXPECT_COMPILE_FAIL)
name = "redundant_cast_same_type"
source = "src"

[artifact]
kind = "binary"
