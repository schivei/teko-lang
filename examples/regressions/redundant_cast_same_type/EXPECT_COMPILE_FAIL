Negative regression (D1, cast-width-hygiene-0.3.1.md): a same-type cast (`x to u32` where `x` is
already `u32`) is a provable no-op — ratified as a DIRECT compile error (no warn phase, owner
ruling 2026-07-24). Proves the class-1 diagnostic is live after the C4 wagon flips it on.
EXPECT_STDERR: redundant cast
