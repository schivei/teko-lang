Negative regression (drop-128, 2026-07-24): a decimal integer literal whose magnitude exceeds
u64::MAX (2^64) has no representable sign-magnitude carrier (NumInt caps at u64) — a hard
parse-time error naming the BigInt literal form (`123bi`) as the honest alternative.
EXPECT_STDERR: integer literal exceeds u64::MAX
