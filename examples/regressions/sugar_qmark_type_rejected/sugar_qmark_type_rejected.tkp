# Regression: 0.3.1 null-union C7 — the `T?` nullable type sugar is removed from the parser. A
# `let y: i64? = null` annotation must be a located parse error naming `T | null`. NEGATIVE FIXTURE
# (asserted to FAIL TO COMPILE).
name = "sugar_qmark_type_rejected"
source = "src"

[artifact]
kind = "binary"
