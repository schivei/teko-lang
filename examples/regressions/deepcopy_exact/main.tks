var b = Box::of(cfg::ORIGINAL)

match deep_copy(b) {
    Box as c => {
        c.set(cfg::MUTATED)
        if b.value != cfg::ORIGINAL { exit(1) }
        if c.value != cfg::MUTATED { exit(2) }
    }
    error => exit(3)
}

exit(0)
