# examples/regressions/shape_constraint_reject — the §9.2b shape-constraint REJECT channel: five
# constraint violations, one per namespace under src/, reported by a single failing build. Each pins a
# refusal the solver must keep making — the `notnull`-in-`|` grammar rule, the honest service-lifetime
# failure (§9.2b: a wrong-lifetime transport FAILS rather than being coerced to singleton), a form-word
# mismatch, a concrete-type mismatch, and a disjunction none of whose alternatives is satisfied. The
# ACCEPT twin is `examples/regressions/shape_constraint`.

name = "shape_constraint_reject"
source = "src"

[artifact]
kind = "binary"
