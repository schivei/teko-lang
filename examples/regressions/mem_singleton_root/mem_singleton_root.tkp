# examples/regressions/mem_singleton_root — the regressor of `#singleton` binding residence
# (0.3.1 modelo-de-memoria-por-escopo §2a, crumb C2b). A `#singleton` local binding is forced to
# root residence by the residence oracle, so it outlives the inner allocating scopes that churn
# around it and reads back its exact value.
name = "mem_singleton_root"
source = "src"

[artifact]
kind = "binary"
