/** entry — `__wrap<T>()` on a non-`ptr`/`uptr` receiver is rejected (§5). */
pub fn entry(): i64 {
    var x: i64 = 5
    match x.__wrap<i64>() {
        i64 as v => v
        error => 1
        null => 2
    }
}
