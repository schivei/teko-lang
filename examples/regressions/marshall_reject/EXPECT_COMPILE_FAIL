This fixture is a NEGATIVE regression: the checker MUST reject it. It is the §5 Part A REJECT host —
seven opaque-`ptr`/`uptr` violations live one per namespace under src/, and the single failing build
reports all seven diagnostics, one per scenario in the .tkr. It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule diagnostics/EXPECT_COMPILE_FAIL declares).

EXPECT_STDERR: é opaco atômico: sem aritmética nem deref
