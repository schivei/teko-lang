This fixture is a NEGATIVE regression (0.3.1 ref-adoption, owner ruling 2026-07-24): a `ref` binding
whose source is an immutable `let` local (`ref r: i64 = x`, `x` a `let`) — rejected by
`type_ref_binding_value` (src/checker/typer.tks): a `ref` binding's source must be a mutable
variable (a `var`) or another reference, never a `let` (deep immutability grants no write-through
handle).

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/diff_vm_native.sh — asserts it fails to compile on BOTH engines (its own COMPILE_FAIL
    list; keep both in sync when adding a negative fixture).

EXPECT_STDERR: cannot take a reference to immutable `x`
