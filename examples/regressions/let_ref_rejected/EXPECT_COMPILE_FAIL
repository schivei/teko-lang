This fixture is a NEGATIVE regression (0.3.1 ref-adoption, owner ruling 2026-07-24): a `let` binding
whose annotated type is a bare reference (`let r: ref i64 = x`) — rejected by `type_binding`
(src/checker/typer.tks): a bare `Reference` bound type is legal ONLY on a genuine `ref r: T = x`
binding (desugared to `BindKind::Mut` by `parse_ref_binding`); `let`/`const` can never carry one —
`let` protects a value deeply, so it can never be a write-through alias.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/diff_vm_native.sh — asserts it fails to compile on BOTH engines (its own COMPILE_FAIL
    list; keep both in sync when adding a negative fixture).

EXPECT_STDERR: `let`/`const` cannot bind a reference
