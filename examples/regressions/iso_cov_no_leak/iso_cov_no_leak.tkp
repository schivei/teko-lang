# examples/regressions/iso_cov_no_leak — the NATIVE-route regressor of E1-C3 hermeticity for a
# natively-lowered Category C singleton, the function-coverage sink (0.3.1 paralelizacao-eixo1). A
# `task_reset()` at each simulated `#test` boundary clears the per-task sink, so a reader test counts
# zero distinct marks whatever the order — the reshuffle-exposed leak cannot occur.
name = "iso_cov_no_leak"
source = "src"

[artifact]
kind = "binary"
