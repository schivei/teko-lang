# crumb_dependency_smoke.tkr — proves the 0.3.1 C4 runner crumb (Background: Given dependency /
# provision_dependency) end-to-end: builds dep/ to a .tkl in a scratch packages/ dir, copies this
# consumer project to scratch, provisions it there, builds it, and runs the result.

Feature: C4 — dependency provisioning

  Background:
    Given dependency = "dep"

  Scenario: the consumer builds against the provisioned dependency and runs
    When built and run
    Then exit = 100
