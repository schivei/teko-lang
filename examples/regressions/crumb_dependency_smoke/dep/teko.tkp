# crumb_dependency_smoke/dep — the DEP half: a `kind = "package"` library exporting one `pub`
# scalar const, consumed bare by the sibling consumer project (0.3.1 C4 crumb smoke test).
name = "crumbdep"
source = "src"

[artifact]
kind = "package"
