Negative regression (drop-128, 2026-07-24): `u128` was removed from the language's surface — the
widest integer is 64-bit (i64/u64). A hard checker error naming the BigInt literal escape hatch
(`123bi`) instead of falling through to a generic "unknown type" message.
EXPECT_STDERR: type 'u128' was removed
