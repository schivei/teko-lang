# Regression (drop-128, 2026-07-24): `u128` is a REMOVED type (0.3.1) — the language's widest
# integer is 64-bit (i64/u64). `scope.tks::builtin_type` honest-stops on the surface name instead
# of falling through to the generic "unknown type" message, naming the BigInt escape hatch
# (`123bi`) as the honest alternative. EXPECT_COMPILE_FAIL.
name = "reject_u128"
source = "src"

[artifact]
kind = "binary"
