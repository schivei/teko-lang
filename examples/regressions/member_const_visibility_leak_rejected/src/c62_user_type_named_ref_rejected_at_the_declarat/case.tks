// F9 romaneio .31 — the audit's compile-fail regressions
// case: user_type_named_Ref_rejected_at_the_declaration
// expected diagnostic: '`Ref` is a reserved type name'
pub type Ref = struct {
    /** any field */
    v: i64
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(0)
}
