// The virtual main of the PARSE-STAGE fold host. It is deliberately trivial and CORRECT: every
// assertion of this project lives in the sibling namespaces, one per rejected construct, and the
// build's job is to fail in the PARSER and report all of their diagnostics at once. A main that
// contributed an error of its own would only add noise to that list.
exit(0)
