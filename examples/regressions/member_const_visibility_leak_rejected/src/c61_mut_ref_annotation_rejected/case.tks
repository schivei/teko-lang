// F9 romaneio .31 — the audit's compile-fail regressions
// case: mut_ref_annotation_rejected
// expected diagnostic: 'a reference binding is written `ref x: T = e`'
pub fn probe() -> i64 {
    mut x: i64 = 1
    mut r: ref i64 = x
    r
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(0)
}
