// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: discard_double_underscore
// expected diagnostic: '`_` is the write-only discard; two or more underscores in a row (`__`)'
pub fn bad() -> i64 {
    __ = 1
    0
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    if bad() != 0 { exit(1) }
}
