# The PARSE-STAGE compile-fail FOLD HOST (NEGATIVE FIXTURE — this project is asserted to FAIL).
#
# NAMING: the member-const visibility regression this directory is named for MOVED to
# examples/regressions/const_ns_qualified_visibility_rejected. A parse error stops the compiler
# before the checker runs, so a checker-stage assertion can never fire in a project whose job is to
# fail in the parser — and the owner's nine-project mandate leaves exactly two failing builds, one
# per phase. The directory keeps its name because it is referenced from the release manifest and CI,
# which this wagon does not touch.
#
# Twenty-four constructs the PARSER must reject live one per namespace under `src/`, sharing this
# single failing build. See the `.tkr` header.
name = "member_const_visibility_leak_rejected"
source = "src"

[artifact]
kind = "binary"
