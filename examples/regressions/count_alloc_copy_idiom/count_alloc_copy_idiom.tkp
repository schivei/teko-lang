# examples/regressions/count_alloc_copy_idiom — the RM-C2 (crumb 0020) no-push output idiom
# fixture. Proves the "count the total, allocate `var final: [total]byte = []` (zero-fill, one
# pass), copy each piece by index" idiom (the base the RM-C3 emit-buffer conversion adopts) builds
# a correct, non-growing buffer out of `teko::runtime::copy` calls alone.
name = "count_alloc_copy_idiom"
source = "src"

[artifact]
kind = "binary"
