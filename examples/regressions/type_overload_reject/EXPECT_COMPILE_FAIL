This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the §9 B type-overload
REJECT host — five illegal forms live one per namespace under src/, and the single failing build
reports every diagnostic it finds, one per scenario in the .tkr: two same-arity declarations differing
only by constraint, a cross-kind same-arity pair, a partially-applied generic, a bare use of a
generic, and a `.{ … }` with no target type. It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule diagnostics/EXPECT_COMPILE_FAIL and match_universal_reject/EXPECT_COMPILE_FAIL declare).

EXPECT_STDERR: duplicate type
