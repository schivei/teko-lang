pub fn wide_product(a: u32, b: u32) -> u64 { (a to u64) * b }

/**
 * entry — F9 romaneio .31 — the audit's positive regressions (backend x scenario)
 *          :: d1_boundary_widen_cast_compiles
 *
 * Unfolded from a `Given source inline` block of regressor.tkr (owner ruling: no inline code in a
 * `.tkr`). It is a MEMBER NAMESPACE of this project and runs off the project's single build,
 * selected by `TEKO_REGR_ENTRY=q185_d1_boundary_widen_cast_compiles`.
 */
pub fn entry() {
    exit((wide_product(7 to u32, 6 to u32) & 255) to i32)
}
