/**
 * entry — a service is always sealed; `virtual service` is rejected at parse (§7).
 *
 * @return i64  never — the build fails
 */
type VClock = virtual service singleton {
    /** @return self  a fresh instance. */
    pub static fn ctor(): VClock { return VClock { } }
}
