/**
 * entry — a service missing the IService `static ctor(): self` factory is rejected (§7).
 *
 * @return i64  never — the build fails
 */
type NoCtor = service singleton {
    /** @return i64  the instant. */
    pub fn now(): i64 { return 0 }
}
