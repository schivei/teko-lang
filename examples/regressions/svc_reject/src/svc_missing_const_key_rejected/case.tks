type MG = interface & IService {
    fn greet(): i64
}

type Only = service singleton & MG {
    pub static fn ctor(): Only { return Only { } }
    pub fn greet(): i64 { return 1 }
}

pub fn entry(): i64 { return svc<MG>("Missing").greet() }
