/**
 * entry — a service is always sealed; `abstract service` is rejected at parse (§7).
 *
 * @return self  never — the build fails
 */
type AClock = abstract service singleton {
    /** @return self  a fresh instance. */
    pub static fn ctor(): AClock { return AClock { } }
}
