This fixture is a NEGATIVE regression: the checker MUST reject it. It is the §7 DI REJECT host —
service/svc violations live one per namespace under src/, and the single failing build reports their
diagnostics, one per scenario in the .tkr. It has no successful build to sanitize/run.

The `svc_plain_interface_rejected` case is the crash anti-regression: `svc<PlainInterface>` used to
SIGSEGV the compiler (the historic dual-registry / ungated locator); it is now a clean type error.

`svc_conflict_same_type_same_key_rejected_{a,b}` (two namespaces, same simple service name, same
implemented interface) is the GAP-A same-type-same-key conflict anti-regression.
`iface_static_instance_mismatch_rejected` is the GAP-D static/instance parity anti-regression.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule diagnostics/EXPECT_COMPILE_FAIL declares).

EXPECT_STDERR: requires T to satisfy IService
