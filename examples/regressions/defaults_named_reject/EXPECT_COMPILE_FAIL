This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the §9 C defaults + `:=`
named-argument REJECT host — seven illegal forms live one per namespace under src/, and the single
failing build reports every diagnostic it finds, one per scenario in the .tkr: a non-trailing default,
a positional after a named argument, an unknown named argument, a named argument colliding with a
positional, a missing required argument, the retired `=` naming surface, and a non-const default. It
has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule diagnostics/EXPECT_COMPILE_FAIL and match_universal_reject/EXPECT_COMPILE_FAIL declare).

EXPECT_STDERR: compile-time constant expression
