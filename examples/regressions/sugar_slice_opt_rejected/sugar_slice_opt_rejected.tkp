# Regression: 0.3.1 null-union C7 — the `T?` sugar is removed even inside a slice element. A
# `let xs: []i64? = ...` annotation must be a located parse error naming `T | null`. NEGATIVE FIXTURE
# (asserted to FAIL TO COMPILE).
name = "sugar_slice_opt_rejected"
source = "src"

[artifact]
kind = "binary"
