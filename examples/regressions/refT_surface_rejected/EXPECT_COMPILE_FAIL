This fixture is a NEGATIVE regression (0.3.1 ref-adoption): a parameter spelled with the literal,
compiler-internal reference type name `Ref<T>` — rejected by the parser's surface-hide gate
(`parse_named`, src/parser/parse_type.tks) before the checker ever runs. `Ref<T>` was the
coexisting pre-adoption spelling (0.3.0.30); adoption hides it everywhere the `ref` keyword grafia
reaches, so this fixture has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/diff_vm_native.sh — asserts it fails to compile on BOTH engines (its own COMPILE_FAIL
    list; keep both in sync when adding a negative fixture).

EXPECT_STDERR: `Ref<T>` is compiler-internal — write the `ref` keyword instead
