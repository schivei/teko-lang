/**
 * h — one arm of an identical-signature `global` pair (§15 ruling 3): its twin in r2_sig_b shares the
 * exact `(name, parameter types)`, so the two collide.
 * @param x the argument
 * @return  x
 */
global fn h(x: i32): i32 { x }
