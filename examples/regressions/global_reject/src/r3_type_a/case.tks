/**
 * Dup — one arm of an identical-name `global type` pair (§15): its twin in r3_type_b shares the name
 * and arity, so the two collide.
 * @field a a field
 */
global type Dup = struct { a: i32 }
