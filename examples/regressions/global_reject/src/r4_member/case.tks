/**
 * Box — a struct whose body illegally marks a method `global` (§15): global reach is namespace-level
 * only, never a type member.
 * @field v a field
 */
type Box = struct {
    v: i32
    global fn oops(): i32 { 1 }
}
