Feature: RT-L6 harness capture — a panic inside capture_panic is caught over the graceful-stop, the suite survives

  Scenario: capture_catches_and_returns — a panicking body is caught with its message; a clean body yields null; the program exits 0
    Given env = ["TEKO_BACKEND=c"]
    When built and run
    Then stdout pattern = "caught: deliberate boom"
    And stdout pattern = "clean-return"
    And exit = 0
