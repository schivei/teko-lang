This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the §4.1 `self { … }`-is-
retired host — the `self { … }` construction form is removed (R2), so a `self { … }` in a method body
is a compile error. The SAME file uses `self` as a RECEIVER (`self._n`) and as a TYPE (`(): self`)
without error, proving only the construction form was retired — `self` remains a receiver and a type.
It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule diagnostics/EXPECT_COMPILE_FAIL and type_overload_reject/EXPECT_COMPILE_FAIL declare).

EXPECT_STDERR: after a statement
