# constraint_form_and_iface.tkr — the SM-G12 (crumb 0018) regression twin: a form-word/interface
# disjunction (`class & Ifce | struct & OtherIfce | str`) already parsed before the crumb and must
# keep parsing/checking unchanged (byte-preserving broadening). `case::entry()` instantiates the
# constrained generic with T = str, satisfying the trailing concrete-type alternative.

Feature: constraint_form_and_iface — a form-word/interface disjunction keeps checking after the broadening

  Scenario: instantiated_with_the_concrete_type_alternative
    When the program is built and run
    Then it exits 0
