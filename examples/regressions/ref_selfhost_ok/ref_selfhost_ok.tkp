# Regression (0.3.1 ref-adoption): `ref` alone (no `Ref<T>`/`.value`/`&x`) drives repeated
# write-through on the same local. This fixture used to prove `Ref<T>`/`.value`/`&x` COEXISTED with
# `ref` (the 0.3.0.30 additive build); adoption hides `Ref<T>` from the surface, so it is repurposed
# to prove the `ref`-only spelling alone reaches the same accumulated exit code.
name = "ref_selfhost_ok"
source = "src"

[artifact]
kind = "binary"
