This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the item-14 value-struct
mutability + readonly REJECT host (§13.1-§13.3) — five illegal forms live one per namespace under
src/, and the single failing build reports every diagnostic it finds, one per scenario in the .tkr:
an external write to a readonly field, an internal `self` write to a readonly field, a write inside a
`readonly struct` method, a mutating method called on a literal receiver, and a value-type method
that writes its readonly `self`. It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule diagnostics/EXPECT_COMPILE_FAIL and defaults_named_reject/EXPECT_COMPILE_FAIL declare).

EXPECT_STDERR: readonly field
