# examples/regressions/trait_fold_reject — the §9.4 composition-fold REJECT host. Two un-absorbable
# same-name method collisions live one per namespace under src/; the fold cuts and names both. A fold
# error is a HARD pre-walk boundary, so these live apart from the checker-stage trait rejects.
name = "trait_fold_reject"
source = "src"

[artifact]
kind = "binary"
