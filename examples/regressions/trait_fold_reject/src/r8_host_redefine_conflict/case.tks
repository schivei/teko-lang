/** A8 — a trait providing `k`. */
type A8 = trait {
    /** k — the trait body. @return 1 */
    fn k(): i64 { 1 }
}
/** H8 — REJECTED: composes `A8` and redefines `k` with a different body (§9.4 no silent override). */
type H8 = struct A8 {
    /** k — the host's differing body. @return 2 */
    fn k(): i64 { 2 }
}
