This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the §9.4 composition-fold
REJECT host — two un-absorbable method collisions live one per namespace under src/. No successful build.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker.

EXPECT_STDERR: differing bodies
