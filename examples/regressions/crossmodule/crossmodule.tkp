# examples/regressions/crossmodule — the project-regressor for the `package` (`.tkl`)
# artifact kind + real multi-module dependency resolution (regressor-principal-0.3.1.md §4
# item 2). Folds the former `const_crossmodule_inline` dep/consumer pair AND supersedes the
# throwaway `crumb_dependency_smoke` C4 crumb smoke test (its own assertion, exit 100, was a
# synthetic placeholder; this fixture proves the SAME `Given dependency` capability end to
# end over the REAL const cross-module chain: serialize -> .tkl -> deserialize ->
# seed_from_dep -> inline_consts -> lower -> run).
name = "crossmodule"
source = "src"

[artifact]
kind = "binary"

[dependencies]
m1 = "0.0.0"
