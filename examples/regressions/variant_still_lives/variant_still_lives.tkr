# variant_still_lives.tkr — the 9D-T1 (crumb 0019) named-`variant`-survives anchor. This crumb is
# purely an ADDITIVE accept for the inline `|` union (field position); `type X = variant A | B`
# still parses and checks unchanged — its removal is 9D-EXP (0094), after every inline site
# migrates.

Feature: variant_still_lives — the named `variant` form is untouched by the inline-union accept

  Scenario: variant_still_parses_and_matches
    When the program is built and run
    Then it exits 0
