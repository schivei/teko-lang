This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the SM-G12 (crumb 0018)
sole-rejected-term anchor — a trait is not a type and stays refused as a constraint term after the
grammar broadens to accept every other type form. No successful build.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker.

EXPECT_STDERR: cannot appear in a constraint
