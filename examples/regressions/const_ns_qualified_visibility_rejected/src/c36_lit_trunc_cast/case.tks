// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: lit_trunc_cast
// expected diagnostic: 'constant out of range for the cast target (M.1 — fail early)'
pub fn bad() -> i64 {
    let x = 256 to u8
    x to i64
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(bad())
}
