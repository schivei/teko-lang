// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: ref_return_local_rejected
// expected diagnostic: 'a `-> ref T` function may return only one of its own `ref` parameters '
fn bad() -> ref i64 {
    mut x: i64 = 5
    &x
}
pub fn probe() -> i64 { bad() }
/** entry — the rejected construct's statement half. */
pub fn entry() {
    (probe()) to i32
}
