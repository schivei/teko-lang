// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: redundant_cast_same_type
// expected diagnostic: 'redundant cast'
pub fn probe() -> i64 {
    let x: u32 = 5
    (x to u32) to i64
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    (probe()) to i32
}
