// F9 romaneio .31 — the audit's compile-fail regressions
// case: lambda_write_to_copy_capture_rejected
// expected diagnostic: 'a lambda cannot assign to the captured variable `n`'
pub fn probe() -> i64 {
    mut n: i64 = 0
    let g = () => { n = 5; 0 }
    g() + n
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(0)
}
