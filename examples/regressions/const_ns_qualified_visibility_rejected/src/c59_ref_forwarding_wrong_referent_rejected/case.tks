// F9 romaneio .31 — the audit's compile-fail regressions
// case: ref_forwarding_wrong_referent_rejected
// expected diagnostic: 'a forwarded `ref` binding must name the SAME referent type'
type Big = struct {
    /** first word */
    a: i64
    /** second word — past the end of an i64 referent */
    b: i64
}
pub fn probe(ref p: i64) -> i64 {
    ref q: Big = p
    q.a
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(0)
}
