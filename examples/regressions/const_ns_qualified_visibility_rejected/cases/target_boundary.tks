// F8 target — the TEKO_TARGET boundary. The SAME trivial program, compiled under three different
// unsupported `TEKO_TARGET` values (the scenarios differ only by `Given target`). It cannot join
// this project's folded build: a differing build CONFIGURATION is a different build by
// construction — the env the compiler runs under is what is under test here.
exit(42)
