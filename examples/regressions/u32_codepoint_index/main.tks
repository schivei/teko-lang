var s = "Hello, Teko!"
var cps = match text::str_to_u32(s) { []u32 as c => c; error => exit(1) }
if cps.len != s.len { exit(1) }
var i: u64 = 0
loop {
    if i >= s.len { break }
    if cps[i] != (s[i] to u32) { exit(1) }
    i++
}
exit(0)
