Negative regression (drop-128, 2026-07-24): `f16` was removed from the language's surface — the
floats are f32/f64. A hard checker error naming `f32` as the honest alternative instead of
falling through to a generic "unknown type" message.
EXPECT_STDERR: type 'f16' was removed
