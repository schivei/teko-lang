# Regression (drop-128, 2026-07-24): `f16` is a REMOVED type (0.3.1) — the language's floats are
# f32/f64. `scope.tks::builtin_type` honest-stops on the surface name instead of falling through
# to the generic "unknown type" message, naming `f32` as the honest alternative.
# EXPECT_COMPILE_FAIL.
name = "reject_f16"
source = "src"

[artifact]
kind = "binary"
