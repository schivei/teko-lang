Feature: mem_free_arena_leak — dropping a #must_free handle without teko::mem::free is rejected

  Scenario: a manual region handle leaked on some path fails the build
    When compilation fails
    Then diagnostic = "is dropped without being freed"
