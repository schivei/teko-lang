/**
 * NoRet — an interface whose getter `value` omits its return type. A getter MUST declare a return
 * type (`get value(): T`); the bare `get value()` is a parse error.
 */
type NoRet = interface {
    /** value — a getter missing its `: T` return type (the rejected construct). */
    get value()
}
