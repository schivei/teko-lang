This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the §9 properties REJECT
host — four property violations live one per namespace under src/, and the single failing build
reports all four diagnostics, one per scenario in the .tkr: writing a read-only property, reading a
write-only property, an implementer missing a required accessor, and a getter with no return type. It
has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule diagnostics/EXPECT_COMPILE_FAIL and match_universal_reject/EXPECT_COMPILE_FAIL declare).

EXPECT_STDERR: read-only
