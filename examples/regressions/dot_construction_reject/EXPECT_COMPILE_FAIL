This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the §4.1 `.{ … }`
target-typed construction REJECT host — two illegal uses live one per namespace under src/, and the
single failing build reports every diagnostic it finds, one per scenario in the .tkr: a `.{ … }` in an
un-annotated binding (no target type), and a `.{ … }` as an `if` scrutinee (no target type). It has no
successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule diagnostics/EXPECT_COMPILE_FAIL and type_overload_reject/EXPECT_COMPILE_FAIL declare).

EXPECT_STDERR: needs a known target type
