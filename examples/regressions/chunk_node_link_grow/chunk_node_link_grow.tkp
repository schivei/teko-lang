# examples/regressions/chunk_node_link_grow — the COL-F0d `chunk_node_link` fixture (crumb 0024).
# Proves the arena's exposed chunk-node capability — the growth point for the future pure-Teko
# `ChunkChain<T>` (Q1) — allocates a FRESH fixed chunk and links it after the given tail via the
# intrusive `CHUNK_NEXT`/`CHUNK_CAP`/`CHUNK_USED` slots, without touching or freeing any existing
# chunk. The compiler holds no `chunk_node_link` callers, so this path is not fixpoint-exercised.
name = "chunk_node_link_grow"
source = "src"

[artifact]
kind = "binary"
