This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the §17 conditional-
compilation region REJECT host — three malformed `#if` regions live one per namespace under src/, and
the single failing build reports every diagnostic it finds, one per scenario in the .tkr: a region
never closed by `#endif`, an `#elseif` illegally following `#else`, and a `#if()` with no predicate.
It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule block_expr_reject/EXPECT_COMPILE_FAIL declares).

EXPECT_STDERR: unterminated `#if` region (expected `#endif`)
