#if(os == "linux")
/**
 * present — a declaration inside a `#if` region that is never closed by `#endif`, so the region
 * parser reports the unterminated region.
 *
 * @return  unreachable — the region is rejected before type-check
 */
pub fn present(): i64 { return 1 }
