This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the 9D-T1 (crumb 0019)
unclosed-grouped-type anchor — `(A | B` in a type position without a closing `)`. No successful
build.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker.

EXPECT_STDERR: expected ')' to close a grouped type
