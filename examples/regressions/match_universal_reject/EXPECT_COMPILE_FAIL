This fixture is a NEGATIVE regression: the checker MUST reject it. It is the match-universal phase 1
REJECT host — four match-over-class/interface violations live one per namespace under src/, and the
single failing build reports all four diagnostics, one per scenario in the .tkr. It has no successful
build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule diagnostics/EXPECT_COMPILE_FAIL declares).

EXPECT_STDERR: non-exhaustive `match`
