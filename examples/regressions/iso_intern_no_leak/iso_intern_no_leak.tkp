# examples/regressions/iso_intern_no_leak — the regressor of E1-C3 test hermeticity for the
# string-intern Category C singleton (0.3.1 paralelizacao-eixo1). A `task_reset()` at each simulated
# `#test` boundary clears the per-task intern table, so a reader test sees MISS whatever the order —
# the reshuffle-exposed leak cannot occur because there is no residue to expose.
name = "iso_intern_no_leak"
source = "src"

[artifact]
kind = "binary"
