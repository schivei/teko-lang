# examples/regressions/value_struct_mut — the item-14 (§13) ACCEPT channel for the MUTABLE value
# struct. One native build carries the four behaviours the fat header + `self`-as-ref buy: an in-place
# mutation that STICKS, value semantics preserved across a copy, an encapsulated setter writing a
# private field, and the `ref` bridge reassigning a whole caller slot. The REJECT twin is
# `examples/regressions/value_struct_mut_reject`.

name = "value_struct_mut"
source = "src"

[artifact]
kind = "binary"
