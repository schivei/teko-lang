This fixture is a NEGATIVE regression: the checker/monomorph MUST reject it. It proves the F4 fix —
that a generic CLASS instantiation is constraint-checked at monomorphization, exactly like a free
generic function. A key type that conforms IEq but NOT IHash cannot instantiate `Dict<Plain, i64>`
(the CLASS), which needs `K: IEq & IHash`; before F4 this escaped to a link error on the missing
`Plain__hash` instead of a clean compile-time stop. It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(scripts/compile_fail_regressions.sh builds it and asserts the build FAILS).
