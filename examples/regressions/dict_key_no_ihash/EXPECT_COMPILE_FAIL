This fixture is a NEGATIVE regression: the checker/monomorph MUST reject it. It proves the fase1b
key gate `K: IEq & IHash` is a REAL constraint, not documentation — a key type that conforms IEq but
NOT IHash cannot be used where the hash-keyed collections require both. It has no successful build to
sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(scripts/compile_fail_regressions.sh builds it and asserts the build FAILS).
