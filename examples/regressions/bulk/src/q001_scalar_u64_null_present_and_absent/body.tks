/**
 * entry — F3 null-union — scalar, struct, class shapes
 *          :: scalar u64 | null, present and absent
 *
 * Unfolded from a `Given source inline` block of regressor.tkr (owner ruling: no inline code in a
 * `.tkr`). It is a MEMBER NAMESPACE of this project and runs off the project's single build,
 * selected by `TEKO_REGR_ENTRY=q001_scalar_u64_null_present_and_absent`.
 */
pub fn entry() {
    let present: u64 | null = 40
    let absent: u64 | null = null
    let rp = match present { u64 as v => v; null => 0 to u64 }
    let ra = match absent { u64 as v => v; null => 2 to u64 }
    exit(rp + ra)
}
