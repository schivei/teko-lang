type Node_iface = struct { v: i64; next: Node_iface | null }
fn sum_list(head: Node_iface | null) -> i64 {
    mut cur = head
    mut total: i64 = 0
    loop {
        match cur {
            Node_iface as n => { total = total + n.v; cur = n.next }
            null => break
        }
    }
    total
}

/**
 * entry — F3 null-union — nesting, base/widen, writeback, forward, recursion
 *          :: self-referential linked-list walk over a null-union next
 *
 * Unfolded from a `Given source inline` block of regressor.tkr (owner ruling: no inline code in a
 * `.tkr`). It is a MEMBER NAMESPACE of this project and runs off the project's single build,
 * selected by `TEKO_REGR_ENTRY=q008_self_referential_linked_list_walk_over`.
 */
pub fn entry() {
    let list = Node_iface { v = 10; next = Node_iface { v = 20; next = Node_iface { v = 12; next = null } } }
    exit(sum_list(list))
}
