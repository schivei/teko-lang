type Sized = trait {
    /** WIDTH — the trait's static member const, folded into every deriver. */
    pub const WIDTH: i64 = 8
}
type Widget_sbmd = struct Sized {
    /** the widget's identifier */
    id: i64
}

/**
 * entry — F2 language corpus — batch 2, the full remaining language/const/ownership corpus (dobrado, dir removido)
 *          :: member_const_trait_fold
 *
 * Unfolded from a `Given source inline` block of regressor.tkr (owner ruling: no inline code in a
 * `.tkr`). It is a MEMBER NAMESPACE of this project and runs off the project's single build,
 * selected by `TEKO_REGR_ENTRY=q120_member_const_trait_fold`.
 */
pub fn entry() {
    if Widget_sbmd::WIDTH != 8 { exit(1) }
    exit(42)
}
