type Point_iface = struct { x: i64; y: i64 }
fn sum_or(p: Point_iface | null, fallback: i64) -> i64 {
    match p { Point_iface as v => v.x + v.y; null => fallback }
}

/**
 * entry — F3 null-union — scalar, struct, class shapes
 *          :: struct Point_iface | null, present and absent
 *
 * Unfolded from a `Given source inline` block of regressor.tkr (owner ruling: no inline code in a
 * `.tkr`). It is a MEMBER NAMESPACE of this project and runs off the project's single build,
 * selected by `TEKO_REGR_ENTRY=q002_struct_point_null_present_and_absent`.
 */
pub fn entry() {
    let present: Point_iface | null = Point_iface { x = 10; y = 20 }
    let absent: Point_iface | null = null
    exit(sum_or(present, 0) + sum_or(absent, 12))
}
