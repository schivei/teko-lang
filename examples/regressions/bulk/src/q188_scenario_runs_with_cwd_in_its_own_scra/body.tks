pub fn probe_qopt() -> i32 {
    match teko::io::write_file("regr_cwd_probe.txt", "romaneio") { error => return 2; null => { } }
    match teko::io::read_file("regr_cwd_probe.txt") { str => { }; error => return 3 }
    match teko::io::read_file("regressor.tkr") { str => return 4; error => { } }
    0
}

/**
 * entry — F9 romaneio .31 — the audit's positive regressions (backend x scenario)
 *          :: scenario_runs_with_cwd_in_its_own_scratch
 *
 * Unfolded from a `Given source inline` block of regressor.tkr (owner ruling: no inline code in a
 * `.tkr`). It is a MEMBER NAMESPACE of this project and runs off the project's single build,
 * selected by `TEKO_REGR_ENTRY=q188_scenario_runs_with_cwd_in_its_own_scra`.
 */
pub fn entry() {
    exit(probe_qopt())
}
