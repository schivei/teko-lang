pub fn build_sum() -> i64 {
    teko::arena_push()
    mut xs: []i64 = teko::list::empty()
    xs = teko::list::push(xs, 10)
    xs = teko::list::push(xs, 20)
    xs = teko::list::push(xs, 30)
    teko::arena_commit()

    mut sum: i64 = 0
    mut i: u64 = 0
    loop {
        if i >= xs.len { break }
        sum = sum + xs[i]
        i = i + 1
    }

    mut ys: []i64 = teko::list::empty()
    ys = teko::list::push(ys, 1)
    sum + ys[0]
}

/**
 * entry — F2 language corpus — batch 2, the full remaining language/const/ownership corpus (dobrado, dir removido)
 *          :: arena_commit_keeps_alloc
 *
 * Unfolded from a `Given source inline` block of regressor.tkr (owner ruling: no inline code in a
 * `.tkr`). It is a MEMBER NAMESPACE of this project and runs off the project's single build,
 * selected by `TEKO_REGR_ENTRY=q022_arena_commit_keeps_alloc`.
 */
pub fn entry() {
    exit(build_sum())
}
