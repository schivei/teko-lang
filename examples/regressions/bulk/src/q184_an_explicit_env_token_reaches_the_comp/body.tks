/**
 * entry — F7 host_cli_io — observable host surface (folded from the retired regressor_crumbs_c0_c4.tkr, dir removido)
 *          :: an explicit env token reaches the compiled program
 *
 * Unfolded from a `Given source inline` block of regressor.tkr (owner ruling: no inline code in a
 * `.tkr`). It is a MEMBER NAMESPACE of this project and runs off the project's single build,
 * selected by `TEKO_REGR_ENTRY=q184_an_explicit_env_token_reaches_the_comp`.
 */
pub fn entry() {
    exit(match teko::env::var("REGR_F7_ENV_PROBE") { str as v => if v == "hello" { 0 } else { 1 }; error => 2 })
}
