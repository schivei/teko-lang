type Animal_iface = virtual class {
    pub name: str
    pub fn make(n: str) -> Animal_iface { Animal_iface { name = n } }
    pub fn label(self) -> str { self.name }
}
type Dog_iface = class Animal_iface(parent) {
    pub fn make(n: str) -> Dog_iface { Dog_iface { name = n } }
    pub fn parent_label(self) -> str { parent.label() }
}

/**
 * entry — F2 language corpus — batch 2, the full remaining language/const/ownership corpus (dobrado, dir removido)
 *          :: class_base_binding
 *
 * Unfolded from a `Given source inline` block of regressor.tkr (owner ruling: no inline code in a
 * `.tkr`). It is a MEMBER NAMESPACE of this project and runs off the project's single build,
 * selected by `TEKO_REGR_ENTRY=q032_class_base_binding`.
 */
pub fn entry() {
    let d = Dog_iface::make("Rex")
    if d.parent_label() != "Rex" { exit(1) }
}
