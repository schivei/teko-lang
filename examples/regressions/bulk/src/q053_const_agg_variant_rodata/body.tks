type RetA_mcni = struct { }
type RetB_mcni = struct { }
type RetC_mcni = struct { }
type Op_mcni = variant RetA_mcni | RetB_mcni | RetC_mcni
const R_mcni: Op_mcni = RetC_mcni { }
fn tag_of(o: Op_mcni) -> i32 {
    match o {
        RetA_mcni => 0
        RetB_mcni => 1
        RetC_mcni => 2
    }
}

/**
 * entry — F2 language corpus — batch 2, the full remaining language/const/ownership corpus (dobrado, dir removido)
 *          :: const_agg_variant_rodata
 *
 * Unfolded from a `Given source inline` block of regressor.tkr (owner ruling: no inline code in a
 * `.tkr`). It is a MEMBER NAMESPACE of this project and runs off the project's single build,
 * selected by `TEKO_REGR_ENTRY=q053_const_agg_variant_rodata`.
 */
pub fn entry() {
    exit((tag_of(R_mcni) + tag_of(R_mcni)) + 8)
}
