// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: member_const_interface_rejected
// expected diagnostic: 'a `const` member is only allowed in a struct, class, or trait body'
type Contract = interface {
    /**
     * run — an ordinary interface method signature (legal).
     *
     * @return an i64
     */
    fn run(self) -> i64
    /** BAD — a `const` member in an interface body: a located parse error (#594 crumb 7). */
    const BAD: i64 = 1
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(0)
}
