// F3 null-union — the strict compile-fail boundary
// case: the retired T? optional-postfix sugar is rejected
// expected diagnostic: 'nullable sugar has been removed'
/** entry — the rejected construct's statement half. */
pub fn entry() {
    let p: ptr<i64>? = null
    exit(1)
}
