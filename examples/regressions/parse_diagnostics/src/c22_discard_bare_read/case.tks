// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: discard_bare_read
// expected diagnostic: '`_` is a write-only discard and cannot be read — it carries no value ('
pub fn bad() -> i64 {
    mut y: i64 = 0
    y = _
    y
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    if bad() != 0 { exit(1) }
}
