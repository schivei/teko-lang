// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: let_ref_rejected
// expected diagnostic: 'a reference binding is written `ref x: T = e`'
pub fn probe() -> i64 {
    mut x: i64 = 1
    let r: ref i64 = x
    r
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(0)
}
