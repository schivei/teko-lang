// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: sugar_qmark_field_rejected
// expected diagnostic: 'safe-navigation operator has been removed'
/** entry — the rejected construct's statement half. */
pub fn entry() {
    let x = 5
    let y = x?.n
    exit(0 to i32)
}
