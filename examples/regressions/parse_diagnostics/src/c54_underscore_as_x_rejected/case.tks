// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: underscore_as_x_rejected
// expected diagnostic: '`_` is a discard and cannot be bound to a name (`_ as x`) — use `_ as '
pub fn f(o: i64 | null) -> i64 {
    match o {
        _ as x => x
    }
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    let n: i64 | null = null
    exit(f(n) + 5)
}
