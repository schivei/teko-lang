// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: sugar_slice_opt_rejected
// expected diagnostic: 'nullable sugar has been removed'
/** entry — the rejected construct's statement half. */
pub fn entry() {
    let xs: []i64? = teko::list::empty()
    exit(0 to i32)
}
