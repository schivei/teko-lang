// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: member_const_override_rejected
// expected diagnostic: 'may not carry `override`'
type Widget = struct {
    /** the widget's identifier */
    id: i64
    /** BAD — a member const bearing `override`: a located parse error. */
    override const BAD: i64 = 1
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(0)
}
