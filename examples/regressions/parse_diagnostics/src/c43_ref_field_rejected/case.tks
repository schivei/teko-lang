// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: ref_field_rejected
// expected diagnostic: 'a reference cannot be stored in a struct/variant/collection'
type Holder = struct {
    /** An illegal stored reference — the compile must fail here. */
    r: ref i32
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(0)
}
