// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: ref_let_binding_rejected
// expected diagnostic: 'a reference binding is written `ref x: T` — the `ref` is itself the bi'
pub fn probe() -> i64 {
    mut x: i64 = 1
    let ref r: i64 = ref x
    r
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    (probe()) to i32
}
