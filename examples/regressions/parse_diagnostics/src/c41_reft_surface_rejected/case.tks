// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: refT_surface_rejected
// expected diagnostic: '`Ref<T>` is compiler-internal — write the `ref` keyword instead'
pub fn probe(r: Ref<i64>) -> i64 {
    0
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(0)
}
