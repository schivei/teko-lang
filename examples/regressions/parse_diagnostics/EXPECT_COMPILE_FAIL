This fixture is a NEGATIVE regression: the PARSER must reject it. It is the PARSE-STAGE compile-fail
FOLD HOST — twenty-four constructs the parser must reject live one per namespace under src/, and the
single failing build reports all twenty-four diagnostics, one per scenario in the .tkr. It has no
successful build to sanitize/run.

NAMING: the member-const visibility regression this directory is named for moved to
examples/regressions/const_ns_qualified_visibility_rejected — a checker-stage assertion cannot fire
in a build that stops in the parser. See this directory's .tkr header.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/compile_fail_regressions.sh — builds it and asserts the build FAILS (issue #610).

EXPECT_STDERR: nullable sugar has been removed
