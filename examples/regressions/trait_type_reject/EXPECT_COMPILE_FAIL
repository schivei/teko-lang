This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the §9.4 trait-not-a-type
REJECT host — a trait in a match case and a trait in a generic constraint. No successful build.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker.

EXPECT_STDERR: discriminant
