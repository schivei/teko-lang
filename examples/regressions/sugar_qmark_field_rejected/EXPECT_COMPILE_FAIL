# Regression (0.3.1 null-union C7): the `?.` safe-navigation operator has been ERADICATED. An
# expression `x?.n` must be rejected at parse time with a diagnostic that names the explicit
# replacement — a `match` on the `T | null` receiver. This project is asserted to FAIL TO COMPILE.
#
# Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
# marker:
#   - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
#   - scripts/compile_fail_regressions.sh — builds it and asserts the build FAILS + the pinned message.
#
EXPECT_STDERR: use an explicit `match`
