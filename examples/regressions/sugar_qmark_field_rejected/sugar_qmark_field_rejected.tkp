# Regression: 0.3.1 null-union C7 — the `?.` operator is removed from the parser. An expression
# `x?.n` must be a located parse error naming an explicit `match`. NEGATIVE FIXTURE (asserted to FAIL
# TO COMPILE).
name = "sugar_qmark_field_rejected"
source = "src"

[artifact]
kind = "binary"
