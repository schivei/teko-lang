This fixture is a NEGATIVE regression: the checker MUST reject it. It is also the CHECKER-STAGE
compile-fail FOLD HOST — thirty-seven rejected constructs live one per namespace under src/, and the
single failing build reports all thirty-seven diagnostics, one per scenario in the .tkr. It has no
successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/compile_fail_regressions.sh — builds it and asserts the build FAILS (issue #610).

EXPECT_STDERR: is private to
