// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: width_rule_int_float_reject
// expected diagnostic: 'no mixed int/float'
pub fn probe() -> i64 {
    let a: i32 = 5
    let b: f64 = 1.0
    a + b
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    (probe()) to i32
}
