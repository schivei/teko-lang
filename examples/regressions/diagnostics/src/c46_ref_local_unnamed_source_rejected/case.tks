// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: ref_local_unnamed_source_rejected
// expected diagnostic: '`let`/`const` cannot bind a reference — a reference binding is spelled'
pub fn bad(c: bool, d: bool, ref p: i64, ref q: i64) -> i64 {
    if c {
        let r = if d { p } else { q }
        r.value
    } else {
        0
    }
}
pub fn mk() -> i64 { 7 }
/** entry — the rejected construct's statement half. */
pub fn entry() {
    (bad(true, true, mk(), mk())) to i32
}
