/**
 * arena_size_on_unsafe — the rejected construct: `#arena_size(N)` is a non-`unsafe` directive by
 * ruling (0.3.1 SW1 crumb `#453`) — it may not precede an `unsafe fn`. This function's whole
 * body executes under the unsafe umbrella, so the annotation is rejected at the declaration
 * site, before the body is even type-checked.
 *
 * @return a value never reached — the enclosing project's build never reaches codegen
 */
#arena_size(4096)
pub unsafe fn arena_size_on_unsafe() -> i64 {
    0
}

/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(0)
}
