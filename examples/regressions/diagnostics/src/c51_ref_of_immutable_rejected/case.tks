// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: ref_of_immutable_rejected
// expected diagnostic: 'cannot take a reference to immutable `x`'
pub fn probe() -> i64 {
    let x: i64 = 1
    ref r: i64 = x
    r
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(0)
}
