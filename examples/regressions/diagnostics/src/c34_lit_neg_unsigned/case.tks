// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: lit_neg_unsigned
// expected diagnostic: 'a negative literal cannot be annotated as an unsigned type (M.1)'
pub fn bad(): i64 {
    var x: u8 = -1
    x to i64
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(bad())
}
