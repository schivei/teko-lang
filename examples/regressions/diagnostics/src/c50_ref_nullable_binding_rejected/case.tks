// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: ref_nullable_binding_rejected
// expected diagnostic: "a `ref` binding's source must be the referent's own type (or another r"
pub fn probe() -> i64 {
    ref r: i64 = null
    r
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    (probe()) to i32
}
