// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: extern_reachability
// expected diagnostic: 'arithmetic needs a numeric operand (int or float)'
pub extern fn c_abs(x: i32): i32 = "abs" from "libc"
pub extern fn phantom_fn(): i32 = "phantom_symbol_181" from "phantomlib"
/** entry — the rejected construct's statement half. */
pub fn entry() {
    c_abs-42 to i32
}
