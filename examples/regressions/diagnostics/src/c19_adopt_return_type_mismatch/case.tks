// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: adopt_return_type_mismatch
// expected diagnostic: "return value does not match the function's declared return type"
pub fn bad_return(): i64 {
    adopt {
        return "hello"
    }
    0
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    if bad_return() != 0 { exit(1) }
}
