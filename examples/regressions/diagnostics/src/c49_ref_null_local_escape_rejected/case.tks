// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: ref_null_local_escape_rejected
// expected diagnostic: 'a function cannot return an optional reference (`Ref<T> | null`) — ref'
fn leak(ref p: i32) -> ref i32 | null {
    p
}
pub fn leak_probe() -> i32 {
    mut x: i32 = 1
    0
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(leak_probe())
}
