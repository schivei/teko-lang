// F9 romaneio .31 — the audit's compile-fail regressions
// case: d1_class3_noop_widen_still_rejected
// expected diagnostic: 'redundant cast'
pub fn probe(a: i32, b: i64) -> i64 { (a to i64) + b }
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(0)
}
