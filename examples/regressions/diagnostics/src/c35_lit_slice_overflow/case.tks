// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: lit_slice_overflow
// expected diagnostic: 'literal out of range for the annotated type (M.1 — fail early)'
pub fn bad(): i64 {
    var xs: []u8 = [1, 300, 3]
    xs[1] to i64
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(bad())
}
