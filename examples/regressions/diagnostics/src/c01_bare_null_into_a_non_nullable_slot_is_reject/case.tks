// F3 null-union — the strict compile-fail boundary
// case: bare null into a non-nullable slot is rejected
// expected diagnostic: 'value type does not match annotation'
/** entry — the rejected construct's statement half. */
pub fn entry() {
    var x: i64 = null
    exit(1)
}
