// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: masked_compound_assign_shift
// expected diagnostic: 'mixed-sign integer arithmetic has no lossless common type here (W-RULE'
pub fn masked_compound_shift_total() -> i32 {
    mut a: i32 = 1 to i32
    a <<= 40

    mut b: u8 = 1 to u8
    b <<= 200

    mut c: u64 = 256 to u64
    c >>= 65

    mut d: u8 = 1
    d <<= 200

    a + b + c + (d to i32)
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    (masked_compound_shift_total())
}
