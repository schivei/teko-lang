// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: ref_null_deref_unnarrowed_rejected
// expected diagnostic: 'cannot dereference a `Ref<T> | null` — narrow it to a bare `Ref<T>` fi'
pub fn deref_probe(): i32 {
    var x: i32 = 7
    var r: ref i32 | null = &x
    r.value
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    exit(deref_probe())
}
