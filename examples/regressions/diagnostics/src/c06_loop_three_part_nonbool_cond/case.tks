// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: loop_three_part_nonbool_cond
// expected diagnostic: 'needs a bool'
/** entry — the rejected construct's statement half. */
pub fn entry() {
    loop var i = 0; i; i++ {
        exit(i)
    }
    exit(0)
}
