// F1 diagnostics — folded EXPECT_COMPILE_FAIL single-file fixtures (dobrado, dir removido)
// case: width_rule_mixed_sign_reject
// expected diagnostic: 'no lossless common type'
pub fn probe() -> i64 {
    let a: u64 = 5 to u64
    let b: i64 = -2
    a + b
}
/** entry — the rejected construct's statement half. */
pub fn entry() {
    (probe()) to i32
}
