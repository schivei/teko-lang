/** §6 REJECT: `RawBuf` (manual-memory surface) was retired in 0.3.1 — use a `[]byte`. */
pub fn holds(b: RawBuf): i64 { 0 }
/** entry — the rejected construct's statement half. */
pub fn entry() { exit(0) }
