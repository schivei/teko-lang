/** dbl — a Family-B comptime doubling a compile-time-constant argument (§14 B3). */
comptime dbl(a: i64): i64 { a + a }
/** entry — the rejected construct: `@dbl(x)` passes a RUNTIME argument, which a comptime cannot fold — the comptime-known rule (§14.2). */
pub fn entry(x: i64): i64 { @dbl(x) }
