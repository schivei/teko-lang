/** loopy — a Family-A macro that expands to a call of itself, so expansion never terminates (§14 A3). */
macro loopy(x) {
    lowering { @loopy(${x}) }
}
/** entry — the rejected construct: `@loopy(0)` recurses past the bounded expansion depth. */
pub fn entry() { exit(@loopy(0) to i32) }
