/** eq_unconstrained — `==` on an UNCONSTRAINED type-parameter `T` (§6): not guaranteed by the constraint. */
pub fn eq_unconstrained<T>(a: T, b: T): bool { a == b }
/** entry — the rejected construct's statement half. */
pub fn entry() { exit(0) }
