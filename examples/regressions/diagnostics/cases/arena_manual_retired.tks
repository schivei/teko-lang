/** §6 REJECT: the non-lexical `Arena` (manual-memory surface) was retired in 0.3.1 — a `{ }` scope is the region. */
pub fn opens(): i64 {
    var a = Arena::new()
    0
}
/** entry — the rejected construct's statement half. */
pub fn entry() { (opens()) to i32 }
