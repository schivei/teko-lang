/** §6 REJECT: `teko::mem::region_alloc` (manual region surface) was retired in 0.3.1. */
pub fn allocs(): i64 {
    var p = teko::mem::region_alloc(0 to uptr, 42 to u32)
    0
}
/** entry — the rejected construct's statement half. */
pub fn entry() { (allocs()) to i32 }
