/** §6 REJECT: the `unsafe type` modifier was retired in 0.3.1 — there is no unsafe surface to mark. */
unsafe type Danger = struct { x: i64 }
/** entry — the rejected construct's statement half. */
pub fn entry() { exit(0) }
