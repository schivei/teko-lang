/** §6 REJECT: the `#must_free` decorator was retired in 0.3.1 — values die at their lexical scope. */
#must_free
pub type Handle = struct { fd: i64 }
/** entry — the rejected construct's statement half. */
pub fn entry() { exit(0) }
