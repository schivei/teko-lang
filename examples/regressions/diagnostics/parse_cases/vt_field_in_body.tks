/** Bad — a value-type body may not declare fields: the value IS the primitive (§9 ruling 10). */
type Bad = i32 {
    /** x — a field, which a value-type body may not carry: the located parse error under test. */
    x: i32
}
/** entry — the rejected construct's statement half. */
pub fn entry() { exit(0) }
