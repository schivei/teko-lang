/** §6 REJECT: the `unsafe fn` modifier was retired in 0.3.1 — there is no unsafe surface to mark. */
unsafe fn poke(): i64 { 0 }
/** entry — the rejected construct's statement half. */
pub fn entry() { exit(0) }
