#os("linex")
/**
 * Y — a const guarded by a MISSPELLED os value (`linex`, the canonical spelling is `linux`). §16
 * crumb 0 rejects it at PARSE time with a located "did you mean" hint (the nearest canonical value by
 * edit distance), the OS-axis twin of the `#arch("aarch64")` guardrail.
 *
 * @return  never — the file fails to compile
 */
pub const Y: i64 = 1
