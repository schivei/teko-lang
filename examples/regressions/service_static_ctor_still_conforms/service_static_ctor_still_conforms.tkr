# service_static_ctor_still_conforms.tkr — the §7 GAP-D regression guard: the new
# `req.is_static != impl_m.is_static` check in `method_sig_matches` must not regress a legitimate
# `static fn ctor(): self` conforming to IService and resolving through `svc<>()`.

Feature: service_static_ctor_still_conforms — a service's static ctor still conforms to IService

  Scenario: svc_resolves_a_singleton_with_a_static_ctor
    When the program is built and run
    Then it exits 42
