This fixture is a NEGATIVE regression: the checker MUST reject it. It is the §7 GAP-C REJECT
counterpart of iservice_constraint_accepts_service: a plain struct (not a service, no IService
conformance) bound to a generic `T: IService` constraint must still fail monomorphization — the
new `IService`-atom predicate in `constraint_atom_satisfied` must not over-accept.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker (the same rule diagnostics/EXPECT_COMPILE_FAIL declares).
