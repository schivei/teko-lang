# cwd_build.tkr — the project-regressor for the #64 CWD-relative invocation shape (0.3.1
# regressor-principal §4 item 7): `cd <project> && teko build .`, the exact shape a real user
# runs from inside their own project, exercised by the `When built in place` runner verb (C3).
# Folds the former `char_ops` fixture and `scripts/native_regressions.sh`.

Feature: cwd_build — CWD-relative `teko build .`

  Scenario: building in place from inside the project directory produces a working binary
    When built in place
    Then exit = 6
