/**
 * A `Svc` class in the `sbmd_left` namespace — the SAME bare name as `sbmd_right::Svc` and `aaa_decoy::Svc`.
 * Its `tag_of` returns the tag unchanged, so `sbmd_left::Svc::make(3).tag_of()` is exactly 3 only when the
 * instance dispatch keys off the CANONICAL `sbmd_left::Svc` (issue #290) rather than the bare `Svc`.
 */
type Svc = class {
    /** The tag this service carries; read back by `tag_of` to observe correct dispatch. */
    pub tag: i64

    /**
     * Construct a `sbmd_left::Svc` with the given tag.
     *
     * @param x  the tag value
     * @return   a `sbmd_left::Svc` carrying `tag = x`
     */
    pub fn make(x: i64): Svc { Svc { tag = x } }

    /**
     * Read this service's tag — the INSTANCE method whose cross-namespace dispatch #290 fixes.
     *
     * @param self  the `sbmd_left::Svc` receiver
     * @return      `self.tag`
     */
    pub fn tag_of(): i64 { self.tag }
}
