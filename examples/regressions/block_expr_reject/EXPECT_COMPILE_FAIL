This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the §9 E Block-B (expression
position, ruling 7) REJECT host — two illegal forms live one per namespace under src/, and the single
failing build reports every diagnostic it finds, one per scenario in the .tkr: a Block-B whose trailing
statement yields no value (an assignment), and a Block-B that DIVERGES (a trailing `return`). It has no
successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule diagnostics/EXPECT_COMPILE_FAIL and match_universal_reject/EXPECT_COMPILE_FAIL declare).

EXPECT_STDERR: must end in a value expression
