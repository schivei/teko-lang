pub fn spread(a: u32, b: u32) -> i64 {
    let d = (a to i64) - (b to i64)
    if d < 0 && (a to i64) < (b to i64) { return 42 }
    0
}

/**
 * entry — F9 romaneio .31 — the audit's positive regressions (backend x scenario)
 *          :: d1_cast_pair_parity_between_arith_and_compare
 *
 * Unfolded from a `Given source inline` block of regressor.tkr (owner ruling: no inline code in a
 * `.tkr`). It is a MEMBER NAMESPACE of this project and runs off the project's single build,
 * selected by `TEKO_REGR_ENTRY=q186_d1_cast_pair_parity_between_arith_and`.
 */
pub fn entry() {
    exit((spread(1 to u32, 2 to u32)) to i32)
}
