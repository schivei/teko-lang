type Animal2 = virtual class {
    /** the animal's display name (unused by the check, kept for shape parity). */
    pub name: str

    /**
     * make — construct a base `Animal2`.
     *
     * @param n  the animal's name
     * @return   a fresh `Animal2`
     */
    pub fn make(n: str) -> Animal2 {
        Animal2 { name = n }
    }

    /**
     * speak — the base's default (overridden by every subclass).
     *
     * @param self  the receiver
     * @return      the base's sound
     */
    pub virtual fn speak(self) -> str {
        "..."
    }
}
type Dog2 = class Animal2 {
    /**
     * make — construct a `Dog2`.
     *
     * @param n  the dog's name
     * @return   a fresh `Dog2`
     */
    pub fn make(n: str) -> Dog2 {
        Dog2 { name = n }
    }

    /**
     * speak — the `Dog2` override.
     *
     * @param self  the receiver
     * @return      "Woof"
     */
    pub override fn speak(self) -> str {
        "Woof"
    }
}

/**
 * entry — F2 language corpus — batch 2, the full remaining language/const/ownership corpus (dobrado, dir removido)
 *          :: union_member_widen_base_first
 *
 * Unfolded from a `Given source inline` block of regressor.tkr (owner ruling: no inline code in a
 * `.tkr`). It is a MEMBER NAMESPACE of this project and runs off the project's single build,
 * selected by `TEKO_REGR_ENTRY=q166_union_member_widen_base_first`.
 *
 * Renamed to keep this project's bare TOP-LEVEL names unique: Animal -> Animal2, Dog -> Dog2.
 * A bare name declared twice in one project is resolved by several compiler passes without
 * consulting the namespace (the #284/#290 defect class), so the fold must not create the clash.
 */
pub fn entry() {
    let a: Animal2 | Dog2 | null = Dog2::make("Rex")
    match a {
        Dog2 as d => exit(0)
        Animal2 as an => exit(41)
        null => exit(42)
    }
}
