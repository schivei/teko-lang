# constraint_service_lifetime.tkr — the SM-G12 (crumb 0018) generic-arg'd-interface-term anchor.
# `<K: service singleton & IChannelKind<T>>` parses: a generic interface term threads `pending_gt`
# through the nested `IChannelKind<T>` close so the outer `<K: …>` list still closes on the `>>`.
# `Fast` (a `service singleton`) satisfies both the lifetime form and the interface term. The
# honest-lifetime reject arm (a non-singleton transport failing the same shape at monomorphization)
# is the pre-existing `shape_constraint_reject/service_transient_where_singleton_rejected` scenario.

Feature: constraint_service_lifetime — a service-lifetime-and-generic-interface conjunction checks

  Scenario: instantiated_with_a_singleton_service
    When the program is built and run
    Then it exits 0
