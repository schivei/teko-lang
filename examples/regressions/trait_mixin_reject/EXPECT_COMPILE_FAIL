This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the §9.4 trait-mixin REJECT
host — illegal trait constructs live one per namespace under src/. It has no successful build to run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker.

EXPECT_STDERR: not a type
