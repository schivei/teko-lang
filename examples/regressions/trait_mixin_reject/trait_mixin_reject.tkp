# examples/regressions/trait_mixin_reject — the §9.4 trait-mixin REJECT host. Each illegal trait
# construct lives one per namespace under src/, and the single failing build reports its diagnostic.
name = "trait_mixin_reject"
source = "src"

[artifact]
kind = "binary"
