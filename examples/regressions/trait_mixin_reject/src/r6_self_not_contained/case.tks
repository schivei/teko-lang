/** ST — REJECTED: a body references `self._x` the trait never declares (§9.4 self-containment). */
type ST = trait {
    /** k — reads an undeclared self member. @return the undeclared member */
    fn k(): i64 { self._x }
}
