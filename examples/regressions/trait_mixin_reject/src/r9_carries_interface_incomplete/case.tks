/** IFoo9 — an interface a trait claims to carry. */
type IFoo9 = interface {
    /** foo — the required method. @return an int */
    fn foo(): i64
}
/** T9 — REJECTED: carries `IFoo9` but implements none of it (§9.4 traits-carry-interface). */
type T9 = trait & IFoo9 { }
