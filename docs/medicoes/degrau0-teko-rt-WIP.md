# Degrau 0 — nativo sem teko_rt.c (WIP)

Religacao dos builtins de logica pura de bytes (rota nativa) aos gemeos Teko
que ja existem em `src/runtime/teko_rt.tks`, no despacho de nome-nu de
`src/lir/lower.tks` (`native_builtin_symbol`).

Escopo: SO logica pura de bytes (Degrau 0). Nada que aloque arena.

Medicoes de reducao de simbolos `tk_*` no `out/teko.c` (antes/depois) seguem
neste diretorio conforme cada religacao e provada.
