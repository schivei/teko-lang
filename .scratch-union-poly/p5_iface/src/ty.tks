type Shape = interface { fn kind(): i64 }
type Circle = class & Shape {
    pub r: i64
    pub static fn make(rr: i64): Circle { Circle { r = rr } }
    pub fn kind(): i64 { self.r }
}
fn f(cond: bool): i64 | Shape { if cond { Circle::make(5) } else { 7 } }
