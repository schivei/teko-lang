// Phase 12 P12-F — multiple nested @dom handle args from source. Each nested call's
// result handle is spilled to its own temp local, then both feed appendChild. Builds
// a <span> and appends it to #out — entirely from a single nested expression.
@dom.appendChild(@dom.getElementById("out"), @dom.createElement("span"));
